import { WorkspaceDesktopRequired } from "@/components/workspace/workspace-desktop-required"

/** Stable, unauthenticated harness for the web→desktop Workspace boundary. */
export default function WorkspaceBoundaryHarnessPage() {
  return <WorkspaceDesktopRequired />
}
