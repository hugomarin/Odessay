# Odessay — Agent Instructions

Lee `AGENTS.md` para las reglas e invariantes del repositorio.
Lee `workflow/agents.md` para operar los comandos `/wf-*` y cargar el contexto correspondiente.

Los skills disponibles en `.claude/skills/` apuntan a sus fuentes canónicas en `.agents/skills/`. Los subagentes de `.claude/agents/` usan los roles canónicos de `.agents/agents/`.
