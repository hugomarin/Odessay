# Capability Integration Map

Canonical inventory of Odessay's integration-level capabilities, their real critical chains, and their **actual** test coverage — audited by reading test bodies, not inferring from filenames.

**Audit snapshot**
```text
Date:            2026-09-19
Audited against: main@b680f0925feb9738a63387ea3aa41bc5e57b89d5
```
`coverage_status` ages from the moment a new test lands. Update the row in place when that happens instead of re-auditing from scratch — but note the new audit date/commit for that row when you do, so stale data is distinguishable from a wrong audit.

## Why this exists

Odessay has ~2000 unit tests. It still ships bugs at the seams between individually-tested pieces — a component, a service and a persistence layer can each be perfectly tested in isolation while the product breaks the moment they run together for real. This map tracks a third axis that unit-test count doesn't measure:

```text
Code coverage        = what code executes during tests
Contract coverage     = which important boundaries are protected
Capability coverage   = which user/system outcomes are proven across their critical chain
```

This document tracks the third. A capability with many unit tests but no integration proof is still only **partially** covered.

**Relationship to other canonical docs (no duplication — read there first):**
- `workflow/testing/critical-capabilities-testing.md` — canonical owner of the test-level taxonomy (Unit/Contract/Integration/E2E/Performance), the "test at the lowest-cost boundary" principle, and when Playwright is/isn't the right tool. This map does not restate those definitions.
- `workflow/quality/capability-proof-contract.md` — **normative companion of this map.** Holds the MUST rules for *constructing* a proof (production-reachable entry point, real production transition sequence, real internal seams, scheduling ≠ completion, deferred-work ownership, full-contract doubles, swallowed errors, mutation test, status-as-output), the escape taxonomy and the pre-upgrade checklist. This map owns *what* must be proven and *how strong* the evidence is; that contract owns *how* the proof is built. Read it before writing or reviewing any proof.
- `architecture/boundaries.yml` + `tests/architecture/*` — Architecture Contracts (how pieces are *allowed* to relate). This map is the complementary layer: Capability Integration Contracts (what those pieces must *accomplish together*). Architecture tests catch "built the wrong way"; this map catches "every piece looks correct, but the product no longer works."
- `workflow/quality/quality-harness-spec-v2.1.md` — the original Quality Harness spec that started this whole rollout (Phases 1-5 of its §10 shipped close to as-written: PRs #433-#436). This map is **not** what that spec's §8 ("Remediation Plane", `architecture/remediation.yml`) describes — that plan was never built. This map is what got built instead, once the team reached that phase: a different mechanism (per-scenario `coverage_status`, not per-gap `disposition`) that ended up carrying the same "prioritize and close what's structurally weak" responsibility. See the spec's own "Estado de implementación" section for the full accounting of what shipped as planned vs. what diverged.

## Coverage status — exactly one per scenario

```text
NONE                No meaningful automated or repeatable evidence exists.
UNIT_ONLY           Individual pieces are tested, but the integration is not.
CONTRACT            The primary boundary invariant is protected (often with the real
                    collaborator faked on at least one side).
PARTIAL_INTEGRATION A meaningful section of the critical chain is tested with real
                    collaborators; another section is mocked/faked internally.
INTEGRATION         The critical chain is exercised with real collaborators
                    sufficiently to prove the capability (E2E via Playwright counts
                    here when it drives the real app, not a packaged bundle).
RUNTIME             The integration also has evidence in the real Tauri runtime.
RELEASE             Validated as part of the distributed artifact/release process.
```

**Mechanical rule:** `Status` is a single value describing the **whole declared chain**, not the best-covered sub-piece. If one segment of the chain is well-proven and another isn't, the row is `PARTIAL_INTEGRATION` (or lower) — the stronger segment goes in `Note`, never in `Status`. This field has to parse unambiguously for a summary rollup or a future agent to consume it; a status like "CONTRACT (server) / NONE (client)" breaks that.

## Priority model

```text
CRITICAL   Product/data integrity or common high-impact behavior.
HIGH       Important user-facing capability with meaningful integration risk.
NORMAL     Useful coverage but lower expected loss/risk.
```

This first pass follows the spec's own v1 suite plus scenarios this audit found sitting at `NONE`/`UNIT_ONLY` with a real data-integrity, security, or silent-failure risk (flagged `(upgraded)` below). Priority will need revisiting as work proceeds — it is not meant to be precise on the first pass, only directionally right.

---

# A. Document Lifecycle

| ID | Capability | Chain | Invariant | Status | Priority | Evidence | Note |
|---|---|---|---|---|---|---|---|
| DOC-01 | Create new document | Editor → identity → draft/session owner | No duplicate identity; no accidental durable file for a truly blank ephemeral draft; identity stable after materialization. | PARTIAL_INTEGRATION | HIGH | `lib/editor/persistence-coordinator.ts` isBodyBlank guard; `tests/editor-persistence-coordinator.test.ts`; `tests/playwright/write-blank-lifecycle.e2e.ts` | Real coordinator runs E2E in-browser, but `createDesktopDraft` is a page-level fake counter — no real filesystem/catalog touched, so "no accidental durable file" is proven by call-count, not disk state. |
| DOC-02 | Materialize first content | first input → createDesktopDraft → DocumentService → filesystem → binding → catalog | Exactly one durable document created; initial content preserved; UUID/binding/catalog/file agree. | INTEGRATION | CRITICAL | `tests/integration/documents/materialize-save-reopen.test.ts` (+ prior: `tests/services/document-service-factory.test.ts`, `tests/playwright/write-new-first-paste.e2e.ts`) | Real `DesktopDocumentService`, `FilesystemDocumentService`, `PersistenceCoordinator` and a real `SqliteDocumentCatalog` class now save/read through real temp-fs, proving SYS-02 (UUID≠path) and the exactly-once catalog write inline — application-side integration with the native Tauri/Rust/SQLite boundary replaced by a real (not spy-based) behavioral test double, not the real database (see structural finding below and SYS-05/SYS-08; `RUNTIME` is reserved for TS → actual `invoke()` → real Rust/SQLite). Mutation-tested live: deleting `persist()`'s `if (fileResult.error) throw` check turns the FAILURE case red for the right reason. Real finding from writing this test, now a documented product decision: a failed materialization can leave a real orphan `.md` file (no catalog row) — accepted as recoverable state owned by the reconciler (SYS-06/07/08), not rolled back by `persist()` itself; the test asserts this explicitly. |
| DOC-03 | Save document | Editor → PersistenceCoordinator → DocumentService → filesystem → catalog → sync queue | No confirmed content lost; success implies durable recoverable state; identity/path stay coherent. | INTEGRATION | CRITICAL | `tests/integration/documents/materialize-save-reopen.test.ts` (+ prior: `tests/document-service.test.ts` web path) | Same real-collaborator stack as DOC-02 (see its note on what's real vs. test-doubled), now entering through the coordinator (matching the declared chain) via `persist()` + `settle()` — the coordinator's own public API for "wait until this document's write has actually landed" — rather than a raw `saveWriting()` call. Verified with a reopen, not just a file read. Includes a RACE variant (see DOC-04). |
| DOC-04 | Sequential saves | save1 → save2 (async, may complete out of order) | The latest valid snapshot wins; an older async completion must not overwrite newer content. | INTEGRATION | CRITICAL | `tests/integration/documents/materialize-save-reopen.test.ts` (DOC-03's RACE case; + prior: `tests/editor-persistence-coordinator.test.ts`) | The mocked-`documentService` version still exists and is more precise for coordinator-internal edge cases; this one proves the same invariant through a real `PersistenceCoordinator` + real `saveWriting` + real fs/catalog, awaiting `coordinator.settle({writingId})` — the coordinator's documented durability contract — rather than inventing a private completion signal, so the test tracks the public API instead of an implementation detail. |
| DOC-05 | Save while switching tabs | Editor A → pending persistence → tab switch → Editor B → return A | Pending A state stays associated with A; the tab switch doesn't cancel/misroute save state; returning to A shows the latest content. | CONTRACT | CRITICAL | `tests/editor-persistence-coordinator.test.ts`; `tests/editor-session-store.test.ts` / `tests/editor-tabs.test.ts` | Well proven at coordinator-logic level with fakes; tab bookkeeping is separately unit-tested as pure state transitions with no I/O underneath. |
| DOC-06 | Close and reopen | save → close → catalog → binding/path → filesystem → hydrate | Reopened content equals the latest confirmed durable content. | INTEGRATION | CRITICAL | `tests/integration/documents/materialize-save-reopen.test.ts` (+ prior: `tests/document-service.test.ts` web path) | Reopen now goes through the real catalog lookup + real file read + real `desktopDocumentEngine.parseSourceDocument`, proving byte-real content survives a save→reopen round trip on desktop, not just web. |
| DOC-07 | Rename document | UUID → DocumentService → catalog → filesystem → binding | UUID stable; path/name changes coherently; catalog and filesystem agree. | PARTIAL_INTEGRATION | HIGH | `tests/filesystem-document-service.test.ts` (in-memory Map fake); `src-tauri/src/commands/workspace.rs` rename/inode tests (real fs+tempdir) | Rust layer genuinely proves identity survives rename on disk; the TS wrapper the app actually calls is Map/mock-tested only — never proven together. |
| DOC-08 | Move document between workspaces | UUID → catalog → filesystem move → workspace index → binding → sync/reconciliation | Same document exists after move; no silent duplicate; old workspace no longer owns the binding; new workspace owns it correctly. | INTEGRATION | HIGH | `tests/integration/documents/cross-workspace-move.test.ts` (ODE-554) | Real `DesktopWorkspaceService.assignToWorkspace` (the real "Assign to Workspace" UI action) → real `relocateDesktopWriting` → real, unmocked `SqliteDocumentCatalog` and `DesktopSettingsService` → two real, distinct temp directories each registered as both a `WorkspaceRecord` and a `BindingRoot` (the real production shape — a Workspace is always both). Proves a document moved twice (managed → A → B) converges to exactly one catalog row at B's path/bindingRootId, the real file exists only at the new path with content intact, A's directory is left empty, **and** (found and fixed on review) A's and B's own workspace *manifests* genuinely converge too — not just the filesystem/catalog. Added `tauriRelocateFileDouble` (real `fs.rename`, mirroring Rust `relocate_file`'s no-op/collision-suffix/never-overwrite semantics) and gave `tauriWorkspaceSyncDouble` real per-root manifest state (see WS-02's note for why that fix was needed). Mutation-tested: a copy-instead-of-move mutation and a skipped-catalog-commit mutation each independently turn the test red. |
| DOC-09 | Import document | file → parser → identity → binding → catalog → document model | Content survives import; identity valid; catalog/path mapping coherent. | NONE | HIGH *(upgraded)* | `tests/import-writing.test.ts` (pure parser unit tests); `lib/services/document-service-factory.ts` `importDesktopWritingFile` — zero test references anywhere | Parser is UNIT_ONLY in isolation; the actual desktop file→identity→binding→catalog wiring (`importDesktopWritingFile`) has no test at all, which is why the whole-chain status is NONE, not UNIT_ONLY. |
| DOC-10 | Archive/delete document | identity → canonical owner → storage/catalog → projections | Correct document affected; no orphan binding; no unrelated content deleted. | PARTIAL_INTEGRATION | HIGH | `tests/services/document-service-factory.test.ts` (mocked); `tests/document-service.test.ts` (web, real localDB); `src-tauri/src/commands/index.rs` real-SQLite delete/restore/purge tests | Rust layer has strong real-SQLite proof; TS desktop orchestration and real filesystem trash-move never exercised together with real collaborators. |

# B. Export

| ID | Capability | Chain | Invariant | Status | Priority | Evidence | Note |
|---|---|---|---|---|---|---|---|
| EXP-01 | Export Markdown | UUID → catalog → content/path → exporter → artifact | Resulting Markdown artifact corresponds to the selected document. | UNIT_ONLY | HIGH | `tests/export.test.ts` (real serializer); `tests/desk/desk-markdown-actions.test.ts` | Real serializer and filename-builder each tested with real content, but nothing threads UUID→lookup→serialize→download as one path. |
| EXP-02 | Export DOCX | UUID → catalog → content → DOCX serializer → binary artifact → filesystem | Artifact exists and is non-empty; content comes from the intended document; a save failure cannot report success. | PARTIAL_INTEGRATION | HIGH | `tests/export.test.ts` (real `docx` lib + JSZip content assertions); `tests/filesystem-document-service.test.ts` (real markdown→real DOCX bytes, Tauri IPC faked) | Real binary generation with real content verification exists for the serializer and the desktop chain; the web API route test mocks the renderer itself, so web-side content fidelity is unproven, and nothing writes real bytes to a real OS file. |
| EXP-03 | Export PDF | document → render/serializer → PDF artifact → filesystem | Valid non-empty artifact; correct document exported; failure surfaces correctly. | PARTIAL_INTEGRATION | HIGH | `tests/export.test.ts` (real react-pdf buffer, `%PDF` magic bytes) | Same serializer-stage pattern as DOCX, but **no desktop-chain test exists for PDF at all** (`filesystem-document-service.test.ts` only exercises `"docx"`) — PDF's full desktop path is unverified. |
| EXP-04 | Export identity resolution | writing UUID → DocumentCatalog.getById → canonicalPath → filesystem adapter | A UUID is never passed as if it were a filesystem path. | CONTRACT | CRITICAL | `tests/services/document-service-factory.test.ts` — AST scan + runtime assertion | Genuinely strong, real protection matching the spec's critical regression contract. Currently well protected, keep watched. |
| EXP-05 | Export failure behavior | export call → dialog cancel / write error → caller | Success is reported only if an artifact was actually written. | PARTIAL_INTEGRATION | CRITICAL *(fixed, one internal seam not yet closed)* | `tests/lib/services/desktop/export-delivery.test.ts` (real `saveDesktopBinaryExport` against a real temp fs, native dialog faked; also real `saveBinaryArtifact` → real desktop dispatch); `tests/components/properties-panel-export.test.tsx` (real `PropertiesPanel.handleExport` reacting to true/false/void/rejected) | **Three real false-success bugs found, all fixed** — see the "EXP-05 (real collaborators)" narrative below for the full account. (1) `editor-shell.tsx`'s `exportBinary()` returned `undefined` (not `false`) on `!currentWritingId` — latent, unreachable via the main editor's own UI guard. (2) `PropertiesPanel.handleExport`'s guard was inverted relative to the invariant (`=== false` vs. the correct `!== true`, given the prop type permits `void`). (3) `app/(app)/desk/page.tsx`'s `exportWritingDocument` discarded `saveBinaryArtifact`'s boolean, and `WritingPreviewModal.handleExportDocument`/`handleExportMarkdown` had no cancel/failure branch at all — **genuinely reachable** via a normal click in Desk's own preview modal, for both the PDF/DOCX and Markdown export items. **Why `PARTIAL_INTEGRATION`, not `INTEGRATION`:** the same rule applied to AI-01 — a boundary internal to the product, not genuinely external, must be connected with real code in the same test, not left as two partial proofs meeting at a mock. `export-delivery.test.ts` proves `saveDesktopBinaryExport` (and, since this review, `saveBinaryArtifact`'s desktop dispatch) against a real fs; `properties-panel-export.test.tsx` proves `PropertiesPanel.handleExport`'s reaction to a *faked* export callback. The hop between them — the real caller (`editor-shell.tsx`'s `exportBinary`/`exportMarkdown`, or `desk/page.tsx`'s `exportWritingDocument`) actually invoking `saveBinaryArtifact` — is never exercised in one test. Declared gap: `real caller → real export orchestration → saveBinaryArtifact → saveDesktopBinaryExport → native boundary fake`; only the last hop should be faked, and today two are. Not closed in this pass — would require rendering `editor-shell.tsx` (a 6000+-line component, same cost class as WATCH-07's deferred `EditorShell` wiring test) or extracting `exportBinary`/`exportMarkdown`/`exportWritingDocument` into testable units, both judged out of scope here. **Second, separate gap:** `WritingPreviewModal`'s own fix (bug 3, both the PDF/DOCX and Markdown variants) has no DOM-level test of its own — driving this component past a real click into `await`ed settlement hits a reproducible `act()` hang, confirmed independent of this change (reproduces with a trivial always-resolves-true fake, and independent of whether `preview` is null or populated) and out of scope to fix here. |

# C. Editor Session & State

| ID | Capability | Chain | Invariant | Status | Priority | Evidence | Note |
|---|---|---|---|---|---|---|---|
| STATE-01 | Text survives tab switch | Editor A edited → switch to B → switch back to A | A returns with the latest content written in A. | INTEGRATION | CRITICAL | `tests/editor-shell-exit-protocol.test.tsx`, `tests/editor-shell-tab-transitions-desktop.test.tsx` | Since ODE-574: shell harness with real internals — real TipTap, real session store, real `.md` writes on desktop. Replaces `tests/editor-shell-tab-switch-persistence.test.tsx`, which mocked TipTap and DocumentService. |
| STATE-02 | Text survives close/reopen | edit → save → close → reopen | Latest confirmed content is restored. | INTEGRATION | HIGH | `tests/playwright/fase4-closure.e2e.ts` | Real browser types, reloads, asserts survival — genuine E2E against the real (unpackaged) app. |
| STATE-03 | Per-document viewport isolation | A scroll=80% → switch to B → B must read `B.savedViewState`/initial, never `A.viewState` | B never inherits A's scroll/view state; canonical concept is `ViewState(documentId)`, not `EditorShell.viewState`. | INTEGRATION | CRITICAL | `tests/playwright/editor-tab-scroll-restore.e2e.ts` (rewritten, ODE-555) | **Revalidated (ODE-555), two prior bugs corrected and a third, real product bug found and fixed.** The stale version (flagged during STATE-05/ODE-551) ran on `/perf/write-harness` (a synthetic, never-persisted `writingId` that tripped `resolveHydrationOutcome`'s real `"unavailable"`-tab-reconciliation path at mount — legitimate behavior for a genuinely orphaned document, just hit here by an id no real user could produce) and used a selector (`button[aria-label^="Open"]`) that never matched a real tab (tabs are `<div role="button">`; the only real match on the page was Next.js's dev-tools toggle) — so "switch back to the original tab" never did. Rewritten on `/perf/editor-harness` (real persisted documents via `forceNewWriting`) with `[data-editor-tab-id]`-keyed selectors (stable, unlike tab titles which collide across multiple "Untitled artifact" tabs), and now includes the sibling-isolation assertion the old version never made (B's scroll must read clean, not inherited from A) — this is the STATE-03 property, and it holds; no cross-document leakage was ever observed in any run. |
| STATE-04 | Restore document viewport | A scroll=80% → B → A | A returns to its own prior viewport when restoration is supported. | INTEGRATION | HIGH | same file as STATE-03 (ODE-555) | **A real, independent, third bug was found and fixed here** (beyond the two harness/selector bugs above): `editor-shell.tsx`'s hydration effect deferred the rich/markdown-mode scroll/selection restore via `window.requestAnimationFrame`, then immediately (same synchronous tick) called `setHydrationWritingId(null)` to mark hydration "done" — but `hydrationWritingId` is this same effect's own dependency, so that state change triggered the effect's cleanup (`generationOwner.cancel(generation)`), racing the still-queued rAF callback with no ordering guarantee. When the cleanup won, `generation.run()` silently no-op'd (by design) and the scroll restore was dropped — confirmed live via `generation.isCurrent()` instrumentation: `true` when the rAF was scheduled, `false` when it fired, on failing runs, with no exception, no remount, no other effect re-run. Reproduced with a live mutation test: reverting the fix (restoring the original unconditional "mark done immediately" ordering) reproduced the drop in 6/8 runs; the fix held 13/13 runs. Fixed by only marking hydration "done" from inside the deepest deferred callback that actually performs a restore (or immediately, for the no-restore-needed case), never before it runs — see `finishHydration()` in the effect. **Follow-up (same PR, from review):** the rich-mode fix above left markdown mode with an equivalent gap — `queueMarkdownSelectionRestore` is a shared, module-ref-based queue with its own internal deferred re-applies (a second, previously ungated `requestAnimationFrame` per scroll axis) and no notion of which document a restore was queued for, so a fast A→B switch could still let A's restore land on B's now-current DOM after `finishHydration()` had already fired. Fixed by threading `isStillValid`/`onSettled` through the queue so hydration only calls `finishHydration()` once the restore has genuinely settled (applied or skipped as stale) — mutation-tested live via `tests/playwright/editor-tab-scroll-restore.e2e.ts`'s new markdown-mode case (reverting to the old "call finishHydration right after queuing" pattern reproduced the failure; the fix held). A second, pre-existing, different-in-kind gap was also found and fixed: the `"open-error"` outcome branch never called `finishHydration()` at all (unlike the adjacent staleness checks, which are fine to skip it since a newer generation already owns `hydrationWritingId` by then) — this left `hydrationWritingId` permanently stuck after any failed open, silently dropping every later retry of that same tab. Mutation-tested live in `tests/editor-shell-tab-switch-persistence.test.tsx` ("ODE-555 follow-up"; since ODE-574 the web `open-error` case of `tests/editor-shell-tab-transitions-desktop.test.tsx`, observing `data-hydration-phase`): asserts `publishTabState`'s own `hydrationWritingId === null` guard lifts again after the failure (observed via the tab's title syncing again in the session store) — reverting the fix left the guard permanently closed and the assertion timed out red; the fix held. The e2e test's scroll assertions were also changed from a single summed `totalScroll` to comparing `editorScrollTop`/`windowScrollY`/`mainScrollTop` individually (with distinct target values per axis), since a summed comparison can't distinguish "restored correctly" from "two axes' values swapped between the wrong document owners" — found on review. |
| STATE-05 | New document starts with clean view state | A has stored view state → create B | B starts clean and never inherits A's scroll/cursor/selection. | PARTIAL_INTEGRATION | CRITICAL | `tests/editor-session-store.test.ts` ("STATE-05 — new tabs never inherit a sibling's view state") | Real `openWritingTab` (web "New Artifact"), real `openDraftTab` on a genuinely new draft slot, and real `openDraftTab` **reusing an existing draft slot under a new ephemeral identity** are all proven directly against the real module-level session store — no mocks beyond `fake-indexeddb`, already this file's precedented external-persistence fake. **A real, reachable bug was found and fixed in the third case:** `handleCreateWorkspaceTab`'s desktop path (`editor-shell.tsx:6073`, also the session-restore fallback at `:1965`) reuses the same `EDITOR_DRAFT_TAB_ID` slot under a fresh `ephemeralDraftWritingIdRef` when the user hits "New Artifact" while already sitting on a still-blank draft — `openDraftTab`'s reuse branch only updated `draft_writing_id`/`last_touched_at` and silently carried the old draft's `scroll`/`selectionFrom`/`selectionTo` forward via `{...tab}`. Reproduced live (confirmed red before any fix), fixed by resetting `view_state` to `DEFAULT_VIEW_STATE` whenever `draftWritingId` actually changes identity (not on a same-identity re-touch, which is asserted separately), mutation-tested (reverting the fix turns only its own assertion red). **Why `PARTIAL_INTEGRATION`, not `INTEGRATION`:** the session-store semantics (what this proof covers) are one hop short of the full user-visible chain. The originally planned E2E version (mirroring STATE-03/04: create B, read the DOM) was attempted first and abandoned — a real React-effect timing race means the hydration effect that would apply a tab's `view_state` to the DOM can complete *before* the newly created tab is even registered in session state, so it never fires for a just-created tab; a mutated (bugged) `openWritingTab` was confirmed to leave that E2E version green regardless of the store's actual correctness. The declared open gap: `real session store (proven) → EditorShell hydration effect → real DOM scroll/cursor (not proven)` — the store→UI seam itself has no automated proof. Closing that is a separate, larger `EditorShell` wiring proof (same cost class as WATCH-07's deferred wiring gap), not attempted here. |
| STATE-06 | Per-document cursor/selection isolation | A selection set → switch to B | Selection state belongs to document identity and cannot leak between documents. | UNIT_ONLY | HIGH *(upgraded)* | `tests/editor-session-store.test.ts` | Only a single tab is exercised — no second tab exists in the test, so cross-document leakage is never checked. Same bug class as STATE-03/04, but with no E2E coverage at all. |
| STATE-07 | Restore cursor/selection | A selection set → switch away → switch back to A | A's own stored selection is restored when supported. | NONE | HIGH *(upgraded)* | none found (restore code is real, in `editor-shell.tsx` ~L2512-2520) | The restore code path exists but has zero test driving an actual switch-away/switch-back and checking the caret lands back correctly. |
| STATE-08 | Hydration never overwrites newer local edits | local edit ↔ async hydration completing later | Older hydrated state cannot replace newer local state. | INTEGRATION | HIGH | `tests/editor-empty-draft-persistence.test.tsx` ("ODE-464"); `tests/editor-hydration-generation.test.ts`; `tests/editor-hydration-coordinator.test.ts` | Real EditorShell forces stale async continuations to resolve *after* switching tabs and asserts none of it touches the new tab — strongest-covered scenario in this cluster. |
| STATE-09 | Rapid tab switching | A → B → C → A (fast) | Document state remains keyed to the correct identity throughout. | PARTIAL_INTEGRATION | NORMAL | same files as STATE-08 | Real generation-ownership races are tested, but only ever one hop (A→B) — no 3+-hop or return-to-origin case exists. |

# D. Annotations & Highlights

| ID | Capability | Chain | Invariant | Status | Priority | Evidence | Note |
|---|---|---|---|---|---|---|---|
| ANN-01 | Add highlight | selection → document model → highlight owner → persistence → projection | Highlight is attached to the correct text range. | INTEGRATION | NORMAL | `tests/playwright/highlight-roundtrip.e2e.ts`; `tests/highlight-annotation.test.ts` | Real browser selects, marks, confirms markdown output; unit file exercises the real TipTap command directly. |
| ANN-02 | Add annotation | selection → annotation owner → document model → persistence → sidebar | Annotation belongs to the correct range; editor and sidebar represent the same annotation. | PARTIAL_INTEGRATION | HIGH | `tests/playwright/floating-overlays-viewport.e2e.ts`; `tests/annotation-bubble-session.test.tsx`; `tests/annotation-document.test.ts` | Each link of the chain has isolated coverage; no single test drives a live selection through confirm to a rendered sidebar entry. |
| ANN-03 | Annotation persistence | create annotation → save → close/reopen | Annotation remains attached to intended content. | PARTIAL_INTEGRATION | HIGH | `tests/playwright/highlight-roundtrip.e2e.ts` (test 2) | Only proves the *absence* of a deleted highlight survives reload — no test proves a still-existing annotation round-trips intact. |
| ANN-04 | Delete typed annotation (ai/personal/footnote/highlight-with-reference) | sidebar delete action → `deleteAnnotation` (real, `lib/editor/footnote-extension.ts:871-921`) → document → persistence → sidebar projection | Annotation disappears everywhere. | INTEGRATION | HIGH | `tests/playwright/annotation-delete-convergence.e2e.ts` ("typed-annotation and standalone-highlight deletes converge to the same final state", ODE-552); `tests/highlight-annotation.test.ts` (table-driven unit coverage of all four types) | **Chain corrected during ODE-552 investigation:** there is no editor-side ("from text") delete UI at all — grepped every editor interaction surface (selection popup, annotation bubble, keyboard, context menu) and found none. Both real delete actions live only in the sidebar's trash button (`notes-panel.tsx:624-645`), forked by an `isStandalone` boolean into two genuinely different functions: `deleteAnnotation` (this row — ai/personal/footnote/highlight-with-reference) and `deleteStandaloneHighlight` (ANN-05). The real, previously-unverified property was never "editor vs. sidebar entry point" — it was whether these two different functions converge to the same final state. Now proven together via real sidebar clicks in one test, checked twice: immediately (DOM mark count + sidebar entry gone) and after a real reload against the durable canonical Markdown itself (anchor text survives, the typed path's `ann-ai` reference token is gone, neither anchor is still wrapped in `==...==`) — the reload check confirms convergence in the persisted representation, not just the rendered DOM. |
| ANN-05 | Delete standalone highlight (no reference node) | sidebar delete action → `deleteStandaloneHighlight` (real, `lib/editor/annotation-highlight.ts:73-89`) → document → persistence → editor projection | Same final state as ANN-04. | INTEGRATION | HIGH | `tests/playwright/annotation-delete-convergence.e2e.ts` (ODE-552); `tests/playwright/highlight-roundtrip.e2e.ts`; `tests/playwright/notes-annotation-pipeline.e2e.ts` | Best-covered annotation scenario — real click, real mark removal, real sidebar removal, survives reload — now proven in the same test as ANN-04's convergent path, not just in isolation. |
| ANN-06 | Edit annotation | edit action → canonical state → all projections | Edited content propagates to canonical state and all projections. | INTEGRATION | NORMAL | `tests/playwright/notes-annotation-pipeline.e2e.ts`; `tests/highlight-annotation.test.ts` | Real app: editing text and type via sidebar both propagate into real markdown source in the same flow. |
| ANN-07 | Sidebar/text consistency | editor annotation state ↔ sidebar annotation state | Both are projections of the same state, not separate sources of truth. | INTEGRATION | HIGH | `tests/playwright/notes-annotation-pipeline.e2e.ts` | Round-trips AI/Personal/Footnote/Highlight/Standalone annotations between Markdown/Rich and shows sidebar matching editor throughout. |
| ANN-08 | Highlight range integrity | edit around highlighted text → re-anchor | Edits must not silently attach the annotation to the wrong content. | PARTIAL_INTEGRATION | NORMAL | `tests/highlight-annotation.test.ts` | Real editor + real re-anchoring logic for one edit shape and one ambiguity case; not tested through persistence, never E2E. |
| ANN-09 | Annotation reopen integrity | annotation exists → reopen/hydration | Annotation exists, anchors correct, sidebar/editor agree after reopen. | PARTIAL_INTEGRATION | NORMAL | `tests/playwright/highlight-roundtrip.e2e.ts` (negative case only) | Only "stays deleted after reopen" is covered — no test confirms a *still-present* annotation keeps correct anchor/agreement after reload. |

**Closed (ODE-552):** ANN-04/ANN-05 convergence. The map's original framing ("delete from text" vs. "delete from sidebar") assumed an editor-side delete UI that doesn't exist in the product — both real delete actions live only in the sidebar's trash button, forked by kind (`isStandalone`) into two different functions: `deleteAnnotation` (typed) and `deleteStandaloneHighlight` (standalone). `tests/playwright/annotation-delete-convergence.e2e.ts` drives both via real clicks in one test and asserts they reach the identical final-state shape (mark/reference gone, sidebar entry gone, anchor text survives a reload with zero marks) — mutation-tested independently for each function (breaking either one's mark-removal step turns red only on that side). See the ANN-04/ANN-05 row notes for the full correction.

# E. Document Metadata

| ID | Capability | Chain | Invariant | Status | Priority | Evidence | Note |
|---|---|---|---|---|---|---|---|
| META-01 | Change status | command/UI → metadata owner → persistence → catalog/query → views → sync | All consumers observe the same status. | PARTIAL_INTEGRATION | HIGH | `tests/desk-workspace-catalog-integration.test.tsx` | Strongest test in the cluster: real Desk + real Workspace mounted over one shared fake catalog, both surfaces observably flip. DocumentService/sync queue still fully mocked. |
| META-02 | Change document type | same pattern as META-01 | Document type persisted and reflected consistently. | UNIT_ONLY | HIGH | `tests/vocabulary/normalize-preserves-unknown.test.ts` (normalizer only) | `changeWritingArtifactType` has real call sites across the app but zero test references anywhere. |
| META-03 | Create status (applied) | Settings → status owner → persistence → selectors → product surfaces | A newly created status is usable end-to-end. | PARTIAL_INTEGRATION | NORMAL | `tests/settings-vocabulary-editing.test.tsx`; `tests/vocabulary/server.test.ts`; `tests/vocabulary/consumers-repaint.test.tsx` | Each layer separately mocked; no single test spans Settings form → API → DB → selector. |
| META-04 | Edit status | same as META-03 | Definition changes do not leave consumers inconsistent. | PARTIAL_INTEGRATION | NORMAL | `tests/vocabulary/end-to-end-contract.test.ts`; `tests/vocabulary/consumers-repaint.test.tsx` | Rename→real-consumer repaint genuinely proven; Settings-form-to-persistence leg is not. |
| META-05 | Remove/disable status | same as META-03 | Documents already using the status transition per an explicit rule; no document enters an invalid state silently. | PARTIAL_INTEGRATION | NORMAL | `tests/vocabulary/consumers-repaint.test.tsx`; `tests/desktop-settings-service.test.ts` | Hide vs delete distinction well tested at orchestration layer; no test proves a real DB/SQLite rewrite or that an already-open document observes the change live. |
| META-06 | Create document type | Settings → type owner → persistence → selectors → assignment UI | A newly created type is usable end-to-end. | PARTIAL_INTEGRATION | NORMAL | same shape as META-03 (`kind="type"`) | Assignment UI never rendered against a live-changed catalog. |
| META-07 | Edit document type | same as META-06 | Existing documents and selectors remain coherent. | PARTIAL_INTEGRATION | NORMAL | `tests/vocabulary/end-to-end-contract.test.ts` | Unlike status, there is **no** real-component repaint test for a type rename — weaker than META-04. |
| META-08 | Remove document type | same as META-06 | Existing documents receive defined behavior; no invalid references remain. | PARTIAL_INTEGRATION | NORMAL | `tests/desktop-settings-service.test.ts` ("Unused" type, 0-row case only) | The desktop rewrite-with-*matching rows* path is only tested for status; the type equivalent has no non-trivial-row test. |

# F. AI Capabilities

| ID | Capability | Chain | Invariant | Status | Priority | Evidence | Note |
|---|---|---|---|---|---|---|---|
| AI-01 | Suggest document title | content/context → context builder → AI service → provider boundary → normalized suggestions → consumer | A valid title-suggestion request reaches the provider whenever the document has content, regardless of sync lifecycle. | INTEGRATION | CRITICAL *(fixed)* | `tests/integration/ai/suggest-title-lifecycle.test.ts` (real `webAIService.suggestTitle` → real `localDB` via fake-indexeddb → real `POST` handler in `app/api/ai/title-suggestions/route.ts`, connected through one global `fetch` router that dispatches by URL instead of a real HTTP server; only the true external provider call is faked); `tests/api/title-suggestions-route.test.ts` (route-only, prior coverage, still valid) | **Reproduced live, confirmed a real bug, fixed.** `checkWritingLifecycleForRemoteAI` hard-blocked `suggestTitle` with `INVALID_INPUT` whenever the local writing's lifecycle was `local-only`/`syncing` — exactly the state of every brand-new draft until its first sync completes, i.e. real users hit this on real documents. Confirmed via `git show 8699899e` that the guard was added (ODE-205) in the same commit and with the same code as the equivalent guard on `hydrateCorrectionBlocks`, where it IS legitimate (correction blocks are keyed server-side by `writingId`) — but `suggestTitle`'s own server route never reads `writingId` at all, so the identical guard had no server-side justification there. Desktop's `AIService.suggestTitle` has no such gate. Fix: removed the lifecycle check from `suggestTitle` only, kept it on `hydrateCorrectionBlocks`, unchanged. **Proof chain, precisely:** real `webAIService.suggestTitle` → real `fetch("/api/ai/title-suggestions")` → real `POST` route handler (real zod request validation, real `hasEnoughTitleSuggestionContent` content check, real prompt construction, real response parsing via `extractTitleSuggestionJson`/`titleSuggestionResponseSchema`) → real `fetch(config.chatCompletionsUrl)` with a real, unmocked `getAIProviderConfig()` (real env-var-driven config, not faked) → **only here**, the actual external AI provider (Fireworks) response is faked. A single global `fetch` mock routes by URL — `/api/ai/title-suggestions` is dispatched to a directly-constructed `Request` passed into the real, imported `POST` function; the resolved provider URL is dispatched to a canned response — so both internal collaborators actually execute, not just the two ends of the chain in isolation. Deliberately kept as deterministic test boundaries (not the AI-01 property, and each needs a live Supabase project to run for real): `getCurrentUserFromRequest` (mocked to a fixed user) and `tryAcquireAiAdmission`/`releaseAiAdmission` (mocked to always admit — the real implementation is a Postgres RPC via `createAdminClient()`). Mutation-tested live: re-added the removed guard, confirmed `local-only`/`syncing` failed with the real route never reached at all (`routeCallCount` stayed 0, not just "an error was returned") across 3 assertions in two test files, reverted, zero diff. Two pre-existing unit tests in `tests/ai-auth-services.test.ts` had encoded the bug as correct behavior since ODE-205 — updated to assert the fixed contract; a clean example of the exact failure mode this initiative exists to catch (unit tests green, real behavior broken). The map's prior open question about `lib/corrections/persistence.ts:90`'s identical-looking pattern is resolved: that one guards `shouldUseRemoteCorrectionPersistence`, the same legitimate correction-blocks-are-server-keyed rationale as `hydrateCorrectionBlocks` — not the same bug, not touched here. **Scope note (unchanged):** this proof covers the invariant as literally stated (request reaches the provider, across all lifecycle values); it does not additionally prove the "consumer" (UI) link — `RenameWritingModal` mocks the AI service entirely (see AI-02's own note), so Suggest→Display with a real, unmocked service response is a separate, already-tracked gap. |
| AI-02 | Apply suggested title | suggestion → canonical rename owner → metadata/filesystem/catalog | Applying an AI suggestion is equivalent to a valid user rename. | PARTIAL_INTEGRATION | HIGH | `components/editor/modals/rename-writing-modal.tsx` (reuses manual-rename `onConfirm`); `tests/filesystem-document-service.test.ts` (real file move + collision suffixing) | Equivalence with manual rename is structurally guaranteed by the UI, and rename itself is solidly integration-tested — but `tests/rename-writing-modal.test.tsx` mocks the AI service and never exercises Suggest→Use→Save as one path. |
| AI-03 | Title suggestion error handling | provider/network failure → error surfaced to UI | Failure must not corrupt document state, leave permanent loading, overwrite the current title, or prevent retry. | NONE | HIGH | Server: `tests/api/title-suggestions-route.test.ts`. Client: none. | Server failure paths (timeout/5xx/lease-release) are covered; client behavior (error display, retry, title-survives) is entirely untested, which is where the actual product-facing invariant lives — hence NONE for the whole chain. |
| AI-04 | Generate corrections | document → correction service/engine → proposals | Proposals reflect real document content. | INTEGRATION | NORMAL | `tests/api/publication-review-route.test.ts` (real prompt building, real token-boundary matching, real learned-word filtering, provider faked) | Strongest AI scenario in the audit — comprehensive error classification and real correction-validation logic against realistic fake provider payloads. |
| AI-05 | Apply correction | proposal → document mutation → persistence | The correct mutation is applied once, to the intended document/range. | PARTIAL_INTEGRATION | HIGH | `tests/publication-suggestion-engine.test.ts` (real markdown mutation); `tests/ai-correction-decorations.test.ts` (real ProseMirror schema/range mapping) | The two real collaborating pieces are each tested with real objects, but `handleAcceptCorrection` in `editor-shell.tsx` — the actual live-editor-transaction + persistence wiring — has no test at all. |
| AI-06 | Reject correction | reject action → correction state update | Rejected suggestions update correction state without altering document content. | UNIT_ONLY | NORMAL | `tests/corrections-admission.test.ts` | Only fingerprint-filtering logic is tested; no test asserts document content is unchanged in a real editor after rejection. |
| AI-07 | Correction persistence/reopen | correction state → save → reopen/hydration | Correction state is restored coherently after hydration. | CONTRACT | NORMAL | `tests/lib/corrections/persistence-lifecycle.test.ts`; `tests/api/corrections-persist.test.ts`; `tests/correction-block-invalidation.test.ts` | Reconciliation/staleness algorithm is real and well-tested, but `localDB`/`webAIService`/Supabase are fully mocked throughout — no real IndexedDB or Postgres round-trip anywhere in this chain. |

# G. Voice

| ID | Capability | Chain | Invariant | Status | Priority | Evidence | Note |
|---|---|---|---|---|---|---|---|
| VOICE-01 | Start recording | permission → MediaRecorder → recording state | Recording state accurately reflects permission/device availability. | PARTIAL_INTEGRATION | NORMAL | `tests/use-voice-recorder.test.tsx`; `tests/playwright/notes-voice-recorder.e2e.ts` | Real hook/UI logic exercised including in a real browser, but `getUserMedia`/`MediaRecorder` are fully faked in every test. |
| VOICE-02 | Stop recording → Blob/MIME | MediaRecorder → Blob → MIME/container metadata | Produced blob has the correct actual media type. | UNIT_ONLY | NORMAL | `tests/voice-recorder-helpers.test.ts` | Every fake `MediaRecorder.stop()` manufactures a Blob using the same type it was told to use — no test proves a *real* recorder's output container matches the requested MIME type. |
| VOICE-03 | Preserve recording | recording completes → retained state | A completed recording is retained until explicitly discarded or successfully consumed. | PARTIAL_INTEGRATION | NORMAL | `tests/annotation-bubble-session.test.tsx`; e2e | Real component state-machine verified at unit+e2e level; recorder/transcription both mocked/faked in both. |
| VOICE-04 | Transcribe recording | Blob → multipart request → transcription service → API → provider boundary → transcript | Transcript reflects the real recorded audio content. | PARTIAL_INTEGRATION | HIGH | `tests/api/margins-transcribe-route.test.ts` (real route+admission, Deepgram `fetch` mocked — acceptable external boundary) | Server segment of the chain is integrated (real route, real admission logic). Client-side wiring (`tests/transcribe-voice-note.test.ts`) is unit-only and never stitched to the real route in one test — per the declared chain (Blob through to transcript), that leaves the whole scenario PARTIAL_INTEGRATION, not INTEGRATION. |
| VOICE-05 | Transcription failure | transcription request → provider/network failure | Failure preserves the recording and exposes retryable state. | PARTIAL_INTEGRATION | HIGH | `tests/annotation-bubble-session.test.tsx`; e2e | Preserves-recording-on-failure genuinely exercised, transcription service faked. |
| VOICE-06 | Retry transcription | failed transcription → retry with retained Blob | The retained original Blob can be retried successfully. | PARTIAL_INTEGRATION | NORMAL | same files | E2E test literally retries twice on one recording; explicit same-blob assertion at unit level. |
| VOICE-07 | Desktop runtime host resolution | desktop runtime → NEXT_PUBLIC_APP_URL/runtime configuration → hosted API | Production desktop points to a reachable hosted runtime. | CONTRACT | HIGH | `tests/transcribe-voice-note.test.ts`; `tests/desktop-runtime-host.test.ts` | Real resolution logic tested, but `fetch`/session mocked; no real Tauri runtime involved. |
| VOICE-08 | Production desktop never uses localhost | production desktop artifact → build/release validation | A production desktop artifact must never contain a localhost runtime URL. | RELEASE | CRITICAL | `scripts/release-desktop.mjs` (scans the actual built `.app`/static-export assets for embedded localhost strings, refuses to publish); `.github/workflows/release-desktop.yml` (real macOS Tauri build on every `app-v*` tag) | **Strongest-covered scenario in the entire map.** A real DMG/.app is built and its shipped JS/HTML scanned before publishing, wired directly into release CI — the bar the rest of the map should aim toward. `scripts/validate-desktop-bundle.mjs` does a deeper DMG/CSP check but is **not wired into any workflow** — manual-only. |

# H. Identity, Catalog & Filesystem

| ID | Capability | Chain | Invariant | Status | Priority | Evidence | Note |
|---|---|---|---|---|---|---|---|
| SYS-01 | UUID resolves to correct document | UUID → DocumentCatalog → binding → canonical document | The same UUID always resolves to the same document. | PARTIAL_INTEGRATION | CRITICAL | `tests/services/document-open.test.ts`; `tests/services/document-service-factory.test.ts`; `src-tauri` index.rs collision test (real SQLite) | Pure-logic contract coverage plus one genuine real-SQLite collision test in Rust; no full-stack proof. |
| SYS-02 | UUID resolves to canonical path | UUID → canonical owner → path | Identity and path are distinct concepts (UUID ≠ path); all filesystem operations resolve through the canonical owner. | CONTRACT | CRITICAL | `tests/services/document-service-factory.test.ts` "filesystem boundary contract" — AST-walks source for every filesystem-delegating method, then runtime-asserts the raw UUID is never passed as identifier | Most rigorously guarded invariant in the whole audit (source-level + runtime), but catalog/filesystem underneath remain mocked — CONTRACT, not INTEGRATION. The spec's critical regression contract; currently well protected, keep watched. |
| SYS-03 | Binding survives rename | rename → binding | Rename changes path/name, not document identity. | PARTIAL_INTEGRATION | HIGH | `src-tauri/src/commands/workspace.rs` (real fs); `tests/services/workspace-reconciler.test.ts` (fakes) | Real disk-level proof in Rust; the TS `SqliteDocumentCatalog` path the app calls in production is mock-only. |
| SYS-04 | Binding survives move | move → binding | Move changes location, not identity. | INTEGRATION | HIGH | `tests/integration/documents/cross-workspace-move.test.ts` (ODE-554) | Same proof as DOC-08: the document's identity (catalog id) is asserted stable while its binding (`bindingRootId`/`canonicalPath`) changes across two real, distinct registered roots — a genuinely different destination each time, not same-root rename-as-move. |
| SYS-05 | Catalog/filesystem consistency | filesystem state ↔ catalog state | Catalog reflects durable filesystem state per the architecture contract. | PARTIAL_INTEGRATION | CRITICAL | `src-tauri` index.rs dual-write/reconcile tests (real SQLite); `tests/contracts/document-catalog.test.ts` (mocked cross-adapter conformance) | Real-SQLite consistency rules are Rust-tested; the TS wrapper the app calls through Tauri IPC is validated only against mocks. |
| SYS-06 | Missing binding behavior | binding lookup fails → explicit recoverable behavior | Missing binding must lead to explicit recoverable behavior — never guess a path. | CONTRACT | HIGH | `tests/services/open-document-desktop.test.ts`; `tests/services/document-open.test.ts` | Repeatedly and explicitly proven at the pure-logic/contract level with fake ports; no real catalog/filesystem exercised. |
| SYS-07 | Ambiguous candidate behavior | multiple filesystem candidates → resolution | Multiple candidates must not silently bind to the wrong document. | CONTRACT | HIGH | `tests/services/workspace-reconciler.test.ts`; `tests/services/document-open.test.ts` | Well-covered "never auto-choose" invariant, entirely with fakes; no equivalent test in the Rust layer (logic lives only in TS). |
| SYS-08 | Reconciliation | divergent catalog/filesystem state → reconciliation | Divergent state converges according to deterministic rules. | PARTIAL_INTEGRATION | HIGH | `tests/services/workspace-reconciler.test.ts` (orchestrator, scanRoot/commit faked); `src-tauri` index.rs convergence/idempotency tests (real SQLite, some across simulated restarts) | Real-SQLite convergence rules strongly proven in Rust; the TS reconciler driving real scanRoot/commit in production is never tested with real dependencies. |

**Structural finding for this group:** every TS-side test that touches the desktop stack mocks the Tauri IPC boundary wholesale, while `src-tauri/src/commands/index.rs` (35 tests) and `workspace.rs` (28 tests) run real SQLite/real filesystem tests for the same logic on the Rust side. Each side is well-tested against a *fake of the other* — the seam itself (TS → real Tauri IPC → real Rust/SQLite/fs) is never exercised together anywhere in the automated suite. This is consistent with the project's known constraint that Tauri IPC isn't measurable client-side (no CDP, no Resource Timing, can't intercept `invoke`) but means the actual production path has no automated proof — only manual DMG evidence, per `tests/fase9-document-catalog-invariants.test.ts`'s own closure matrix (which self-reports several rows as `PENDING`/manual and should not be read as independent automated evidence for any scenario above).

# I. Sync

| ID | Capability | Chain | Invariant | Status | Priority | Evidence | Note |
|---|---|---|---|---|---|---|---|
| SYNC-01 | Local save enqueues sync | save local → sync mutation | Exactly the expected mutation is created. | PARTIAL_INTEGRATION | HIGH | `tests/sync-service.test.ts` (web, real fake-indexeddb queue); `tests/desktop-catalog-sync-service.test.ts` (desktop, SQLite write mocked); `src-tauri` real-rusqlite tests | Web path has real local persistence; desktop's real-SQLite half (Rust) and JS-invocation half are each tested but never joined. |
| SYNC-02 | Successful sync acknowledgment | cloud ack → sync state update | Acknowledgment updates sync state without changing intended document content. | CONTRACT | NORMAL | `tests/desktop-catalog-sync-service.test.ts` | Both boundaries mocked — invariant asserted, never against real persisted rows. |
| SYNC-03 | Sync failure preserves local content | local state → failed sync attempt → local state | Cloud/network failure must never destroy or blank local content. | PARTIAL_INTEGRATION | CRITICAL | `tests/integration/sync/sync-worker-failure-preserves-local-content.test.ts` (web, ODE-553); `tests/desktop-catalog-sync-service.test.ts` ("SYNC-03: a failed sync attempt leaves the real .md file on disk byte-identical", ODE-553) | **Web proof (`INTEGRATION`-grade on its own): a real, reachable bug found and fixed, in two passes.** Real `SyncWorker.flush()` against a real `localDB` (`fake-indexeddb`), only the network transport faked. `processMutation` optimistically writes `lifecycle: "syncing"` before the network call. First fix reverted it only in the terminal-failure branch (retries exhausted) — **caught in review as still incomplete**: a *retryable* failure left `lifecycle` on `"syncing"` too, so the next attempt's own pre-attempt snapshot (captured fresh each call) already read `"syncing"`, meaning the terminal branch's `!== "syncing"` guard never fired either — the bug survived the whole retry sequence, not just the last attempt. A direct-seeded test (mutation `attempts` jumped straight to the terminal boundary) missed this because it starts from a lifecycle never touched by a prior failure. Reproduced live at each stage (terminal-only red before any fix; still red under the terminal-only fix when driven through a real multi-attempt sequence). Fixed by reverting `lifecycle` on **every** failure, retryable or terminal — `"syncing"` now means "a remote attempt is actively in flight," not "a retry is scheduled" (that's already covered by `sync_status`/the queue's own `attempts`/`next_retry_at` and the `"retrying"` event) — re-fetching current state each time and patching only `lifecycle`, never clobbering a newer save. Since `WritingLifecycle` has only three values and real call sites (`hydrateCorrectionBlocks`) gate on it never being `local-only`/`syncing`, this was a genuine, permanent, cascading block on anything gated by lifecycle for any document whose sync ever exhausted retries. Mutation-tested twice: reverting to no revert at all, and reverting to the terminal-only version, each reproduces red at the exact assertions that distinguish them. Also proven for `server-confirmed` documents (reverts to `server-confirmed`, not `local-only`) and for the precise terminal boundary (`attempts === MAX_ACTIVE_RETRIES - 1`) in isolation. Body content itself was never at risk in this path (never written on failure) — confirmed, not assumed. **Desktop proof: no bug found, structurally safe.** `record.binding.canonicalPath`'s `.md` file — the actual content substrate for a filesystem-bound document (the SQLite catalog only caches metadata) — is read-only in this service; a real temp file (swapped in for the mocked `tauriOpenFile`) is re-read byte-for-byte after a forced Supabase failure and found untouched. Mutation-tested live: a synthetic file-blanking write added to the failure branch turned this test red, reverted. **Why the row stays `PARTIAL_INTEGRATION`, not `INTEGRATION`:** the desktop proof's `SqliteDocumentCatalog` and Supabase client remain mocked (this test file's existing, pre-established pattern) — only the file substrate was upgraded to real; per this map's own mechanical rule, the row's status reflects the weaker of its two platform chains, not the stronger. |
| SYNC-04 | Retry is idempotent | failed mutation → retry | Retry must not duplicate or corrupt content/state. | PARTIAL_INTEGRATION | HIGH | `tests/sync-worker.test.ts`; `desktop-catalog-sync-service.test.ts`; Rust real-SQLite retry-budget test | Real-DB retry bookkeeping and JS upsert-fallback logic each solid, tested in isolation from each other. |
| SYNC-05 | Multiple local saves before sync | save1 → save2 → … → sync | Final synced state represents the correct latest local version. | PARTIAL_INTEGRATION | CRITICAL | `tests/sync-worker.test.ts`; Rust real-SQLite supersede test | Same split-chain pattern as SYNC-04 — real logic in Rust, mocked logic in JS, never combined. |
| SYNC-06 | Conflict handling | local version ↔ remote version → resolution | Conflict resolution must never silently discard a version without following explicit policy. | PARTIAL_INTEGRATION | HIGH | `tests/sync/hydrate-local-writings-from-remote.test.ts`; `tests/fase9-document-catalog-invariants.test.ts` (row B8.5 self-flagged `PENDING`/manual) | Version-based LWW over real IndexedDB + faked network is genuinely tested; the harder concurrent-edit/explicit-policy case is honestly flagged by the team itself as unautomated. |
| SYNC-07 | Offline → online recovery | queued mutations (offline) → connectivity restored → processed | Queued mutations survive offline state and process correctly when connectivity returns. | UNIT_ONLY | HIGH | `tests/sync-worker.test.ts`; `tests/desktop-catalog-sync-service.test.ts`; closure row B8.4 self-flagged `PENDING`/manual | No real network layer or process restart; deferred to manual DMG evidence by the team's own DoD. |
| SYNC-08 | Metadata-only mutation does not blank content | metadata-only update → mutation payload | A metadata-only update must not send or persist an empty document body. | CONTRACT | CRITICAL | `tests/desktop-catalog-sync-service.test.ts` — three targeted tests with explicit `not.toHaveProperty` assertions | Best-tested invariant in the sync cluster; Supabase/SQLite both mocked — no real-DB round trip. |

# J. Workspace / Filesystem

| ID | Capability | Chain | Invariant | Status | Priority | Evidence | Note |
|---|---|---|---|---|---|---|---|
| WS-01 | Mount workspace | filesystem state → coherent workspace/catalog representation | Mounted workspace state matches real filesystem state. | PARTIAL_INTEGRATION | HIGH | `src-tauri/src/commands/workspace.rs` (real temp fs); `tests/services/workspace-reconciler.test.ts` (fakes); `tests/desk-workspace-catalog-integration.test.tsx` (real UI mount, faked catalog) | Real fs→manifest half and real UI→catalog half each tested; SQLite catalog itself is fake in both. |
| WS-02 | Move file between workspaces | same as DOC-08, workspace-owner perspective | Filesystem, workspace indexes, bindings, and catalog all agree after the move. | INTEGRATION | HIGH | `tests/integration/documents/cross-workspace-move.test.ts` (ODE-554) | Same proof as DOC-08, driven through the actual `DesktopWorkspaceService.assignToWorkspace` entry point (real UI-reachable action) rather than calling `relocateDesktopWriting` directly — the workspace-owner's own perspective on the same convergence. A B→A round trip is also proven (no orphan/duplicate on a second real move). **A real gap was caught on review before merge:** the first version proved physical move + catalog convergence but not the Workspace *index/manifest* itself — `relocateDesktopWriting`'s origin-root resync call (`tauriWorkspaceSync(sourceRootPath)`, no explicit ids) was silently throwing inside the old double (`Object.entries(undefined)`) and getting swallowed by production's own recoverable-error `catch {}`, so the source-manifest-drop step never actually ran in the test. Fixed by giving `tauriWorkspaceSyncDouble` real per-root manifest state: an explicit-ids call durably binds entries; a no-ids call (production's real "rescan/reconcile this root" form) drops any entry whose file no longer exists on real disk — mirroring a genuine directory rescan. The test now separately asserts, via that same real call shape, that the source root's manifest no longer claims the document and the destination's does, at the exact path the catalog agrees on. Mutation-tested: disabling the reconcile-delete step, and skipping the bind-merge step, each independently break the test (the latter breaks even earlier, at initial document creation, since the managed root's own registration depends on the same merge). |
| WS-03 | External rename | filesystem external change → watcher → reconciler → catalog → UI projection | External rename is reconciled without losing or duplicating identity. | PARTIAL_INTEGRATION | HIGH | Rust real-fs rename test; `workspace-reconciler.test.ts` (fake); closure row B8.2 self-flagged `PENDING`/manual | Overlaps with WATCH-03 — same evidence. |
| WS-04 | External create | same as WS-03 | A newly created external document becomes discoverable according to policy. | PARTIAL_INTEGRATION | HIGH | `workspace-reconciler.test.ts` (fake); Rust manifest-layer real-disk discovery | Overlaps with WATCH-01. |
| WS-05 | External delete | same as WS-03 | Catalog/UI update coherently without corrupting unrelated data. | PARTIAL_INTEGRATION | HIGH | `workspace-reconciler.test.ts`; Rust real-fs/real-SQLite delete tests | Overlaps with WATCH-05. |
| WS-06 | Workspace switch isolation | Workspace A state ↔ Workspace B state | Workspace A state must not contaminate Workspace B. | NONE | HIGH *(upgraded)* | none found — existing e2e switches sidebar *panels* ("Contents"/"Workspace"), not two Workspace *roots* | Largest structural blind spot in this cluster: no test anywhere drives two different BindingRoots/Workspaces through a real move or checks cross-workspace isolation. |
| WS-07 | Selected paths / watcher scope | configured workspace path scope → watcher/catalog behavior | Watcher and catalog must honor configured workspace path scope. | PARTIAL_INTEGRATION | NORMAL | `tauri-fs-watch.test.ts`; `workspace-reconciler.test.ts` (pure); Rust real-fs scope tests | — |

# K. Status / Type Configuration

| ID | Capability | Chain | Invariant | Status | Priority | Evidence | Note |
|---|---|---|---|---|---|---|---|
| CONFIG-01 | Create status (schema) | Settings → configuration owner → persistence → selectors → editor/Desk/filters | Newly created status definition is usable end-to-end. | PARTIAL_INTEGRATION | HIGH | `tests/settings-vocabulary-editing.test.tsx`; `tests/vocabulary/server.test.ts`; `tests/desktop-settings-service.test.ts` | Three independently-mocked layers each unit/contract-tested; never chained with real persistence. |
| CONFIG-02 | Rename status | same as CONFIG-01 | Rename preserves the underlying key/identity of the status. | PARTIAL_INTEGRATION | NORMAL | `tests/vocabulary/consumers-repaint.test.tsx`; `tests/vocabulary/end-to-end-contract.test.ts` | Key-stability-by-design is good architecture; proven end-to-end only from catalog snapshot onward, not from the Settings form. |
| CONFIG-03 | Disable/delete status | same as CONFIG-01 | Disable/delete follows an explicit, safe transition rule. | PARTIAL_INTEGRATION | NORMAL | same as META-05 | — |
| CONFIG-04 | Create document type | same as CONFIG-01 | Newly created type definition is usable end-to-end. | PARTIAL_INTEGRATION | HIGH | same as META-06 | — |
| CONFIG-05 | Rename document type | same as CONFIG-01 | Rename preserves the underlying key/identity of the type. | PARTIAL_INTEGRATION | NORMAL | `tests/vocabulary/end-to-end-contract.test.ts` | Weaker than CONFIG-02 — no repaint test exists for a type rename (see META-07). |
| CONFIG-06 | Delete document type | same as CONFIG-01 | Delete follows an explicit, safe transition rule. | PARTIAL_INTEGRATION | NORMAL | same as META-08 | Type-delete-with-matching-docs branch untested on desktop. |
| CONFIG-07 | Existing documents survive schema change | schema rename/delete → rewrite (real Postgres fn / real `DesktopSettingsService` over a behavioral catalog double) → existing documents | A schema rename/delete must never corrupt or orphan documents already using the old value. | INTEGRATION | CRITICAL | `supabase/tests/delete_vocabulary_item_rewrite.test.sql` (real Postgres function against a real local DB, via pgTAP); `tests/integration/vocabulary/schema-change-safety.test.ts` (real `DesktopSettingsService` against a real behavioral catalog test-double) | Both sides have direct proof, precisely framed: Postgres side is real Postgres (`delete_vocabulary_item` run against a real local Supabase/Postgres instance via `supabase test db --local`) — proves the rewrite is scoped by `author_id`/RLS, not just by matching status text (a second user's writing with the *same* status string is unaffected), and that base-item/not-found rejections use the right SQLSTATE. Desktop side is the real, unmodified `DesktopSettingsService.deleteVocabularyItem` running over a real behavioral native-storage test double (not "real SQLite" — that would be `RUNTIME`-tier Rust/cargo evidence, still open, see the branch-coverage follow-up below) — proves the matching-rows rewrite, the non-matching-rows-untouched case, and a FAILURE case (a failed bulk write leaves the catalog at prior values *and* the vocabulary item still defined). Mutation-tested live on both: the Postgres test caught a `security definer` + missing-scope regression (3 rows rewritten instead of 2, cross-user corruption); the desktop test caught a dropped key-filter regression (rewrites every row instead of just matches). Not wired into CI (no pgTAP test in this repo is yet — a pre-existing gap, not introduced here; see the "pgTAP CI wiring" follow-up below). **Scope caveat:** both proofs exercise only the `kind = status` deletion branch; the `kind = type` branch (→ rewritten to `general`) shares the same function/service but has no test of its own yet — see the CONFIG-07 branch-coverage follow-up in the Phase 3 tracker. |
| CONFIG-08 | Selectors/filtering reflect new definitions | same as CONFIG-01 | Selectors/filters reflect the current schema definition. | PARTIAL_INTEGRATION | NORMAL | `tests/vocabulary/consumers-repaint.test.tsx` | One consumer surface proven reactive; `filter-bar.tsx`, `artifact-type-selector.tsx`, `properties-panel.tsx` read the same catalog by architecture but have no equivalent test. |

# L. Sharing

| ID | Capability | Chain | Invariant | Status | Priority | Evidence | Note |
|---|---|---|---|---|---|---|---|
| SHARE-01 | Enable sharing | document → sharing owner → API/cloud → authorization → consumer view | Sharing state change is durably persisted and takes effect. | UNIT_ONLY | HIGH | `tests/api/sharing-cors.test.ts`; `tests/sharing-service.test.ts`; `tests/desktop-sharing-service.test.ts`; `tests/writing-shares.test.ts` | Every layer unit-tested with the adjacent layer mocked; no test performs a real Supabase write. |
| SHARE-02 | Disable sharing | same as SHARE-01 | Same as SHARE-01, in reverse. | UNIT_ONLY | HIGH | same as SHARE-01 | — |
| SHARE-03 | Generate/open preview link | same as SHARE-01 | A generated link resolves to the correct document under the intended authorization. | UNIT_ONLY | HIGH | `tests/sharing-service.test.ts`; `tests/api/margins-preview-route.test.ts`; `tests/playwright/preview-shared-margins.e2e.ts` | Richest unit/contract set in the cluster, but the e2e test intercepts the preview API entirely rather than resolving a real token — generation and lookup are never connected. |
| SHARE-04 | Permission enforcement | same as SHARE-01 | Only authorized consumers can access a shared/unshared document. | PARTIAL_INTEGRATION | CRITICAL | `supabase/tests/writing_shares_permission_enforcement.test.sql` (real Postgres RLS + real `list_incoming_shared_writings` security-definer RPC, via pgTAP against a real local instance); prior: `tests/writing-shares.test.ts` | Two independent authorization paths behind the same property, both real Postgres and both proven: (1) `writings`/`writing_shares` table RLS via `can_read_writing` — a stranger (authenticated or anonymous) cannot read a private or shared-with-someone-else writing even knowing its id, but genuinely can read a public one (positive control, including for `anon`); only the owner can create or revoke a share grant — proven both ways (owner's insert succeeds under RLS, grantee's insert throws `42501`, grantee's delete affects zero rows) — and revoking a share actually removes access, not just the row. (2) The separate security-definer RPC desktop's "shared with me" list uses — re-implements its own `auth.uid()` scoping instead of relying on table RLS, and a scoping bug there is a full cross-user leak with nothing else to catch it. Mutation-tested live on all of the above: loosening `can_read_writing` to treat `shared` like `public` was caught; dropping the RPC's `shared_with_id` filter was caught; loosening the delete policy to `using (true)` was caught; breaking the insert policy to `with check (false)` was caught. **Still not proven:** several web paths (`/shared/[id]`, `listSharedWritingsForUser()`, `web-sharing-service.ts`, `/api/writings/import`) authorize via `createAdminClient()` (service role) with hand-written checks instead of relying on RLS — RLS is bypassed there by design, and this proof does not reach that path. The declared chain is `document → sharing owner → API/cloud → authorization → consumer view`, i.e. the full product outcome, not just the DB substrate — so the scenario stays `PARTIAL_INTEGRATION` until the service-role path has its own real proof (or the scenario is deliberately re-scoped to "database authorization enforcement" with a sibling scenario created for the service-role path — not done, to avoid the capability quietly coming to represent a technology instead of an outcome). |
| SHARE-05 | Visibility changes persist | same as SHARE-01 | A visibility change persists across sessions/reloads. | NONE | HIGH *(upgraded)* | none found | — |
| SHARE-06 | Reopen preserves sharing state | same as SHARE-01 | Reopening a document shows its correct current sharing state. | NONE | NORMAL | none found | — |

# M. Collections

| ID | Capability | Chain | Invariant | Status | Priority | Evidence | Note |
|---|---|---|---|---|---|---|---|
| COL-01 | Add document to collection | document → collection membership → persistence | Membership is durably persisted. | UNIT_ONLY | NORMAL | `tests/api/writings-collections-route.test.ts` (Supabase fully mocked) | `replace_writing_collections` (real, security-definer, ownership-checked SQL) is never exercised by a JS test. |
| COL-02 | Remove document from collection | same as COL-01 | Same as COL-01, in reverse. | UNIT_ONLY | NORMAL | same file | — |
| COL-03 | Multiple collection membership | same as COL-01 | A document can belong to multiple collections simultaneously without conflict. | UNIT_ONLY | NORMAL | `tests/collections.test.ts`; confirmed real (non-vestigial) feature via schema + component read | Only pure-function/mocked-RPC coverage — no test proves two real collections and one real writing persist simultaneously through the DB. |
| COL-04 | Reopen preserves membership | membership set → reopen/hydration | Membership survives reopen/rehydration. | INTEGRATION | NORMAL | `tests/collections-remote-bootstrap.test.ts` (real fake-indexeddb + real localDB + real hydration merge; only network mocked) | Genuinely tests two real collaborating pieces. |
| COL-05 | Collection queries reflect mutations | mutation → query | A collection query reflects the most recent mutation. | PARTIAL_INTEGRATION | NORMAL | `tests/collections.test.ts`; `tests/collections-remote-bootstrap.test.ts` | Local-store read-after-write is real; server-side equivalent is untested (GET route test mocks Supabase entirely). |
| COL-06 | Delete collection without corrupting documents | delete collection → member documents | Deleting a collection must not delete or corrupt its member documents. | NONE | HIGH *(upgraded)* | `app/api/collections/[id]/route.ts`; schema `ON DELETE CASCADE` on the join table only | The invariant is schema-guaranteed by design but has zero test coverage — no test calls `DELETE` and asserts a writing survives. |

# N. External Watcher Behavior

| ID | Capability | Chain | Invariant | Status | Priority | Evidence | Note |
|---|---|---|---|---|---|---|---|
| WATCH-01 | External create | filesystem → watcher → reconciler → catalog → mounted product state | Same as WS-04. | PARTIAL_INTEGRATION | HIGH | same evidence as WS-04 | — |
| WATCH-02 | External edit | same as WATCH-01 | An external edit to a file is reconciled into the mounted state correctly. | PARTIAL_INTEGRATION | HIGH | Rust real-fs+hash tests; reconciler fake | `tauri-fs-watch.test.ts`'s self-write-suppression tests are the *inverse* case (ignoring the app's own writes), not a genuine external edit reaching the UI. |
| WATCH-03 | External rename | same as WATCH-01 | Same as WS-03. | PARTIAL_INTEGRATION | HIGH | same evidence as WS-03 | — |
| WATCH-04 | External move | same as WATCH-01, across BindingRoots | Same as SYS-04, across BindingRoots. | UNIT_ONLY | HIGH | `workspace-reconciler.test.ts` inode-correlation (fake, within one root only) | **Not closed by ODE-554 (DOC-08/WS-02/SYS-04's proof), on reflection — a genuinely different code path.** That proof exercises the app *itself* relocating a file via `relocateDesktopWriting`; WATCH-04 is about a file moved *externally* (e.g. in Finder) between two watched roots and correctly reconciled by the external-watcher/reconciler path (`WATCH-01`'s chain), which never calls `relocateDesktopWriting` at all. Still no test at any level drives a real external move across two distinct registered BindingRoots — this remains open. |
| WATCH-05 | External delete | same as WATCH-01 | Same as WS-05. | PARTIAL_INTEGRATION | HIGH | same evidence as WS-05 | — |
| WATCH-06 | Ambiguous reconciliation | same as WATCH-01 | Ambiguous external state must never silently bind to the wrong document. | UNIT_ONLY | NORMAL | `workspace-reconciler.test.ts` | Pure fakes, no real fs/DB. |
| WATCH-07 | Active document changes externally | document open/dirty in editor → real watcher → real reconciler → real catalog → editor reaction → write-side hash precondition | No save may durably overwrite a durable external edit unless the user explicitly chose to replace it. | PARTIAL_INTEGRATION | CRITICAL *(fixed, wiring not yet proven)* | `src-tauri/src/commands/document.rs` cargo tests (4, including a live stale-hash-refused-and-disk-untouched case); `tests/integration/documents/watch07-external-content-conflict.test.ts` (real `PersistenceCoordinator` + real `FilesystemDocumentService`, write-side hash check, including a SEQUENTIAL same-document-two-saves proof); `tests/editor-external-change-policy.test.ts` (pure CLEAN/DIRTY decision + the reason filter) | **First investigated, not assumed: the watcher IS live.** Initial static trace only followed `DesktopWorkspaceService.watchWorkspace()`/`watchWorkspaces()` (dead code — real callers were removed in ODE-373, `067cee59`, with no replacement) and wrongly concluded no watcher runs at all. Corrected: the actual, currently-live path is `DesktopAppShell` → `useWorkspaceReconciler` → `ensureWorkspaceReconciler` (`desktop-workspace-reconciler.ts`) → `configureRootWatcher` → real `watchFsPaths` (Tauri `plugin:fs\|watch`), mounted once for the app's whole lifetime. An external content edit genuinely reaches `catalog.applyReconcileTransaction` → `CatalogChange(reason:"bulk")` → `editor-shell.tsx`'s own `subscribeToCatalog` handler for the open document — confirmed by reading every step, not assumed. **The actual gap:** that handler only ever compared `localPresent`/`canonicalPath` (rename/delete) — never `content_hash` — so a pure content edit reached the editor and was silently ignored; `ExternalFileNotice`'s union type had no `"content-changed"` variant at all. Combined with a second, independent finding — **no write anywhere in the stack (`PersistenceCoordinator` → `FilesystemDocumentService.saveWriting` → Rust `write_file`) had any precondition on the file's current on-disk content** — this meant a subsequent save would always silently clobber whichever side lost the race, in either direction. **Fix, three layers:** (1) `write_file` (Rust) takes `expected_content_hash: Option<String>` and refuses the write with a `CONFLICT:`-prefixed error (reusing the existing `content_hash_for_markdown_file`/blake3 primitive `workspace_sync` already uses) if the file's real current hash doesn't match, checked immediately before the atomic rename. (2) `PersistenceCoordinator` — not the caller — owns the durable-content-hash baseline (`getDurableContentHash`/`setDurableContentHash`), advancing it only after a real durable commit (from the write's own authoritative `WritingRecord.contentHash`, itself sourced from the same manifest-sync hash the catalog binding already tracks) and resolving it fresh at the moment each write actually executes, never frozen into a caller's snapshot. (3) `editor-shell.tsx` compares the catalog's live `content_hash` against the coordinator's tracked baseline via `resolveExternalContentChange`, which also filters by `CatalogChange.reason`: only `"bulk"` (the reconciler's own reason) counts as external evidence — the app's own saves emit `"content"`/`"upsert"` for the same document and must never be mistaken for an external edit. CLEAN (no pending local edit) auto-reloads with a brief notice; DIRTY raises a blocking conflict banner (`persistEditorSnapshot` refuses to autosave while it's active) with two explicit actions — "Reload external" / "Keep my version" (the latter re-seeds the baseline to the exact external hash first, then persists normally — no separate force-bypass code path). No automatic merge. **Two real races found and fixed on review, both against the first version of this fix:** (a) the original design threaded a `baselineContentHash` field through `PersistenceSnapshot` itself, frozen at `persist()`-call time — a second save queued behind an in-flight first save would read the *pre-first-save* baseline once it finally ran, misreading its own predecessor's just-landed write as an external conflict and refusing to save (confirmed live: reverting coordinator ownership back to a frozen-snapshot design made the new SEQUENTIAL proof fail with a false `CONFLICT`); fixed by moving baseline ownership fully into the coordinator, resolved fresh at write time. (b) the original design also emitted no `reason` filter, so a document's own normal autosave (`CatalogChange(reason:"content")`) could be mistaken for external reconciliation; fixed by requiring `reason === "bulk"` (confirmed live: removing the filter made 7 decision-function assertions wrongly resolve "conflict"/"auto-reload" for the app's own catalog changes). (c) A third race found on a further review pass: `PersistenceCoordinator.hasPending()` alone under-reports "is there an unsaved edit" — the desktop persistence debounce (150ms rich mode, `MARKDOWN_SAVE_DEBOUNCE_MS` markdown mode) means there is a real window right after a keystroke where the editor already holds an edit that has not reached `persist()` at all yet, so `hasPending()` still (correctly, from its own narrower view) reports nothing pending; an external `bulk` `CatalogChange` arriving in exactly that window would classify the document CLEAN and auto-reload over the unsaved keystroke. Fixed with a second, independent signal — `hasUnconfirmedLocalEditRef`, set the instant a real edit happens (TipTap's own `onUpdate` for rich mode, each markdown-mutating handler's own entry point for markdown mode — never on a programmatic `setContent`, already guarded by the pre-existing `isApplyingContentRef`) and cleared the instant that content is actually handed to `persist()` (from which point `hasPending()` is authoritative) — combined via a new pure `computeHasPendingLocalEdit(unconfirmed OR pending)` in `external-change-policy.ts` rather than an untested inline `||`. All three mutation-tested and reverted, zero diff. **Scope note — why this stays `PARTIAL_INTEGRATION`:** the write-side guarantee (no silent overwrite, ever) is now fully proven at the real coordinator/service/Rust layer, including both races above. What is *not* yet proven is that `editor-shell.tsx`'s own reactive wiring — a real `CatalogChange` arriving → the real effect calling `getDocumentService().openWriting()` → `editor.commands.setContent()` → the conflict banner's two real button handlers — actually does the right thing when exercised end to end; that would need rendering the full `EditorShell` (or a smaller extracted controller/hook, deliberately not attempted in this pass) and is deferred rather than rushed. Until that wiring has its own test, the capability stays one notch below `INTEGRATION` even though the property it exists to protect (silent data loss) is fully closed at the layer that actually enforces it. |

**Structural note (confirmed across WS/WATCH):** the overlap between WS-03/04/05 and WATCH-01/03/05 is real, not incidental — they're covered by the *exact same* tests. None of those tests exercise the full chain (real fs → real watcher event → real reconciler decision → real SQLite commit → real UI) in one place: `workspace_sync` in Rust never touches the SQLite catalog; `reconcileRoot` in TS never touches real fs or real SQLite; the Rust `catalog_apply_reconcile` test exercises real SQLite but with hand-built (not fs-derived) input. Several test names reference specific ODE incidents and do correctly test the general capability, not just the narrow historical repro (spot-checked: ODE-404, ODE-408, ODE-453, ODE-460) — this is a case where regression-test naming did *not* mean narrow-only coverage.

---

# Audit Summary (Phase 1 + Phase 2 complete)

**106 scenarios inventoried** across 14 capability categories (A–N, sections above). This is the full sweep from the specification, not a subset.

## Coverage breakdown

| Status | Count | % |
|---|---|---|
| NONE | 7 | 7% |
| UNIT_ONLY | 14 | 13% |
| CONTRACT | 9 | 8% |
| PARTIAL_INTEGRATION | 54 | 51% |
| INTEGRATION | 21 | 20% |
| RUNTIME | 0 | 0% |
| RELEASE | 1 | 1% |

*(Mechanically re-verified against the table below by parsing each row's Status column — not hand-counted, every time a status changes. This table went stale once, for one PR cycle: PR #438 upgraded DOC-02/03/04/06 without updating it here — see the git history of this file for the correction.)*

**The headline finding matches the reported pain point exactly:** roughly half of all scenarios (53/106) sit at `PARTIAL_INTEGRATION` — real collaborators tested on one side of a boundary, fakes on the other, never joined. This is precisely the shape of bug this initiative exists to catch: every piece passes its own tests, the seam between them doesn't. Zero scenarios have been validated against the real packaged Tauri runtime (`RUNTIME` = 0); only one (`VOICE-08`) reaches `RELEASE`.

## Critical gaps (NONE or UNIT_ONLY on a CRITICAL/HIGH-priority scenario)

1. **STATE-07 — Cursor restore.** Same bug class as the already-partially-addressed scroll-position issue (STATE-03/04), but with no test at all. `NONE`, `HIGH`.
2. **WS-06 — Workspace switch isolation.** No test drives two real Workspace roots at once. `NONE`, `HIGH`.
3. **COL-06 — Delete collection without corrupting documents.** Schema-guaranteed, never tested. `NONE`, `HIGH`.
4. **DOC-09 — Import document (desktop).** The actual wiring function has zero test references anywhere in the repo. `NONE`, `HIGH`.
5. **SHARE-05 / SHARE-06 — Visibility persistence / reopen.** `NONE`, `HIGH`/`NORMAL`.

*(CONFIG-07 closed — see below. SHARE-04 moved from `NONE` to `PARTIAL_INTEGRATION` — real DB-substrate proof, service-role path still open. AI-01 moved from `NONE` to `INTEGRATION` — reproduced live, confirmed a real bug, fixed. WATCH-07 moved from `NONE` to `PARTIAL_INTEGRATION` — real watcher/reconciler chain confirmed live (an earlier static trace wrongly concluded it wasn't), a real silent-overwrite gap found and fixed with a write-side hash precondition (including two real races caught on review), but `EditorShell`'s own reactive wiring still has no automated test. EXP-05 moved from `NONE` to `PARTIAL_INTEGRATION` — three real false-success bugs found and fixed (two latent, one reachable via Desk's preview modal), but the internal seam between the real callers and the proven `saveBinaryArtifact`/`saveDesktopBinaryExport` layer is still faked in every test (same rule as AI-01's internal-seam finding), and the Desk-modal fix has no automated wiring test of its own (pre-existing test-harness limitation, not this fix). STATE-05 moved from `NONE` to `PARTIAL_INTEGRATION` (ODE-551) — all three real "New Artifact"/draft-reuse call sites proven clean at the store level against a live sibling tab's non-default view state, including a real bug found and fixed in the draft-slot-reuse case, all mutation-tested; a store-level test, not the originally-planned E2E mirror of STATE-03/04, after a genuine React-effect timing race was found to make the E2E version unable to observe the risk at all — the store↔`EditorShell` DOM-application seam remains an open follow-up (see the STATE-05 row note). All removed from this list since none are `NONE`/`UNIT_ONLY` any more; see each row and the "Remaining P0 items" note below.)*

## Existing strong coverage (hold the line here)

- **VOICE-08 — Production desktop never localhost.** `RELEASE`. A real DMG is built and scanned before every publish, wired into CI. The model to imitate elsewhere.
- **EXP-04 / SYS-02 — UUID never used as a path.** `CONTRACT`, protected by an AST scan *and* a runtime assertion. The single most rigorously guarded invariant in the codebase.
- **STATE-08 — Hydration never overwrites newer local edits.** `INTEGRATION`, real EditorShell forcing real stale-async races.
- **ANN-05/06/07 — Sidebar/editor annotation convergence.** `INTEGRATION` via real E2E flows spanning Markdown/Rich modes.
- **AI-04 — Generate corrections.** `INTEGRATION` up to a deterministic provider fake — the correct shape for AI capabilities generally.
- **SYNC-08 — Metadata-only mutation safety.** `CONTRACT`, three explicit negative-shape assertions.
- **COL-04 — Reopen preserves collection membership.** `INTEGRATION`, real IndexedDB + real hydration merge.
- **CONFIG-07 — Existing documents survive schema change.** `INTEGRATION` — real Postgres RLS-scoped rewrite and real `DesktopSettingsService` over a behavioral catalog double, with a cross-user RLS proof and a bulk-write-rejected-before-apply proof. Covers only the `status` branch so far (see the branch-coverage and bulk-rollback follow-ups in the Phase 3 tracker).
- **SHARE-04 — Permission enforcement (database substrate only).** `PARTIAL_INTEGRATION` — table RLS via `can_read_writing` and the separate `list_incoming_shared_writings` security-definer RPC are both real-Postgres proven (a stranger, authenticated or anonymous, genuinely cannot read a private or shared-with-someone-else writing, and genuinely can read a public one; only the owner can create/revoke a grant). Not `INTEGRATION` yet: the web app's service-role authorization paths (`/shared/[id]`, `listSharedWritingsForUser()`, `web-sharing-service.ts`, `/api/writings/import`) bypass RLS by design and are not covered by this proof — see the SHARE-04 row note.
- **AI-01 — Suggest document title.** `INTEGRATION` — reproduced a real, live bug (client-side lifecycle gate blocking every brand-new draft's title suggestion until first sync) and fixed it; real `webAIService` → real `localDB` → the real route handler, connected through one URL-routed `fetch` mock, with only the actual external AI provider faked. Consumer-level (UI) coverage with a real, unmocked AI service response remains a separate open gap under AI-02.
- **WATCH-07 — Active document changes externally.** `PARTIAL_INTEGRATION` — corrected a wrong initial finding (the watcher IS live, confirmed by tracing `DesktopAppShell` → `useWorkspaceReconciler` → real `watchFsPaths`), found the real gap (the editor's catalog subscription ignored content changes; no write anywhere had a hash precondition), and closed it with a write-side conflict guard reused from the same blake3 primitive `workspace_sync` already relies on — real Rust `cargo test` coverage plus a real `PersistenceCoordinator`↔`FilesystemDocumentService` integration proof, including a second review pass that caught and fixed two real races (baseline ownership, own-save-reason filtering). Not `INTEGRATION` yet: `EditorShell`'s own reactive wiring (real `CatalogChange` → real reload/conflict UI) has no automated test of its own — see the WATCH-07 row's scope note.
- **EXP-05 — Export failure behavior.** `PARTIAL_INTEGRATION` — real `saveDesktopBinaryExport`/`saveBinaryArtifact` proven against a real temp filesystem (dialog cancel, real write success, a genuine fs write failure via a blocker-file parent path) and real `PropertiesPanel.handleExport` proven to react correctly to true/false/void/rejected. Three real false-success bugs found and fixed, one genuinely reachable (Desk's preview modal, both PDF/DOCX and Markdown). Not `INTEGRATION`: the hop between the real export callers and the proven `saveBinaryArtifact` layer is still faked in every test (an internal seam, per the same rule AI-01 established), and `WritingPreviewModal`'s own fix has no DOM-level test — see the EXP-05 row for both gaps.
- **STATE-05 — New document starts with clean view state.** `PARTIAL_INTEGRATION` — all three real "New Artifact"/draft-reuse paths (`openWritingTab` for web, `openDraftTab` on a fresh slot, `openDraftTab` reusing an existing draft slot under a new identity) proven directly against the real session store. Found and fixed a real bug in the third case: the reused-draft-slot path silently carried the old identity's scroll/selection forward; now resets to clean on identity change. Mutation-tested for all three. Not `INTEGRATION`: the store↔`EditorShell` DOM-application seam has no automated proof (a genuine React-effect timing race makes a naive E2E version of this test unable to observe the risk at all — see the row note), so only the store-semantics half of the invariant is proven.
- **ANN-04/ANN-05 — Annotation-delete convergence.** `INTEGRATION` — corrected a wrong premise in the map itself: there is no editor-side ("from text") delete UI in the product, only the sidebar's trash button, forked by kind into `deleteAnnotation` (typed) and `deleteStandaloneHighlight` (standalone). One real test now drives both via real clicks and proves they converge to the same final-state shape (mark/reference gone, sidebar entry gone, survives reload), mutation-tested independently for each function.
- **DOC-08/WS-02/SYS-04 — Move a document between two real Workspace roots.** `INTEGRATION` — the first test to actually move a real file between two real, distinct, registered BindingRoots (both a `WorkspaceRecord` and a `BindingRoot` each, the real production shape), through the real reachable UI entry point (`DesktopWorkspaceService.assignToWorkspace`). Added the one missing real-collaborator piece the existing double infrastructure needed (`tauriRelocateFileDouble`). Proves single-row catalog convergence, real file relocation (moved, not copied-and-orphaned), and a clean round trip back. Mutation-tested against a copy-instead-of-move bug and a skipped-catalog-commit bug. `WATCH-04` (the same gap for an *externally*-triggered move, a genuinely different code path) remains open.
- **SYNC-03 — Sync failure preserves local content.** `PARTIAL_INTEGRATION` — real bug found and fixed on the web path, across two review passes: `SyncWorker` left a document's `lifecycle` permanently stuck on `"syncing"` once retries exhausted, since the optimistic pre-attempt write was never reverted. A first fix reverting only on terminal failure was caught in review as still broken for the real multi-retry sequence (a retryable failure already left it stuck, poisoning every later attempt's own baseline) — fixed properly by reverting on every failure. Reproduced live at each stage, mutation-tested against both the no-revert and terminal-only-revert versions. Desktop path proven safe by construction (the real `.md` file substrate, not the mocked SQLite catalog, is confirmed untouched on failure) — the row stays `PARTIAL_INTEGRATION` because the desktop catalog/Supabase collaborators remain mocked, per this map's rule that status reflects the weaker platform chain.
- **STATE-03/STATE-04 — Per-document viewport isolation and restore.** `INTEGRATION` — revalidated after being flagged `STALE`; two prior test bugs corrected (a never-persisted synthetic harness id, and a tab-reselection selector that never matched any real element) surfaced a third, real, independent product bug: a hydration-generation race in `editor-shell.tsx` where marking hydration "done" cancelled the same generation whose deferred `requestAnimationFrame` scroll-restore callback hadn't fired yet, silently dropping the restore in ~30-50% of runs. Fixed by only marking hydration done from inside the deepest deferred callback that actually restores, never before. Mutation-tested live: reverting the fix reproduced the drop in 6/8 runs; the fix held 13/13.

## V1 Implementation Plan (ordered — ties to the spec's own §10 plus the upgrades above)

1. ~~CONFIG-07 real-engine test~~ — **done**, see `supabase/tests/delete_vocabulary_item_rewrite.test.sql` + `tests/integration/vocabulary/schema-change-safety.test.ts`.
2. SHARE-04 real RLS/ownership test — **database substrate done**, see `supabase/tests/writing_shares_permission_enforcement.test.sql`; the web service-role authorization path (`web-sharing-service.ts` and callers) is a separate, still-open follow-up before this can move to `INTEGRATION` (see the SHARE-04 row note).
3. ~~Reproduce and, if confirmed, fix the AI-01 `local-only`/`syncing` lifecycle gate, with a regression test~~ — **done**, see `tests/integration/ai/suggest-title-lifecycle.test.ts`. Confirmed real, fixed.
4. WATCH-07 — external edit to an open/dirty document — **write-side fix done**, see `tests/integration/documents/watch07-external-content-conflict.test.ts` + `src-tauri/src/commands/document.rs` cargo tests; confirmed real, fixed, and two follow-on races caught on review are fixed too. Still open: a test of `EditorShell`'s own reactive wiring (or an extracted controller/hook), needed before this can move past `PARTIAL_INTEGRATION`.
5. EXP-05 — export failure paths (dialog cancel, write failure) — **write-side + UI-reaction proofs done**, see `tests/lib/services/desktop/export-delivery.test.ts` + `tests/components/properties-panel-export.test.tsx`; confirmed three real false-success bugs (two latent, one reachable) and fixed all three. Still open: connect a real caller (`exportBinary`/`exportMarkdown`/`exportWritingDocument`) to `saveBinaryArtifact` in one test (internal seam, not yet closed), and a `WritingPreviewModal` DOM-level wiring test (blocked on a pre-existing `act()` hang) — needed before this can move past `PARTIAL_INTEGRATION`.
6. ~~DOC-02/DOC-03/DOC-06 desktop path~~ — **done**, see `tests/integration/documents/materialize-save-reopen.test.ts` and the Phase 3 progress tracker below.
7. STATE-05 — clean-slate view state on a newly created tab — **store-level portion done, `PARTIAL_INTEGRATION`**, see `tests/editor-session-store.test.ts` ("STATE-05 — new tabs never inherit a sibling's view state"); confirmed real for `openWritingTab`, `openDraftTab` on a fresh slot, and `openDraftTab` reusing an existing draft slot under a new identity — the last one found and fixed a real bug (the reused slot silently kept the old identity's scroll/selection), mutation-tested all three. Landed at the store level instead of the planned E2E mirror of STATE-03/04 after finding the E2E version can't observe the risk at all (see the row note). Still open before `INTEGRATION`: a proof of the store→`EditorShell` DOM-application seam. STATE-06/07 (cursor/selection isolation and restore) remain open too — same bug class, still no test.
8. SYS-05/SYS-08 — one TS-side integration test with real SqliteDocumentCatalog + real fs, replacing at least one of the fully-mocked contract tests.
9. ~~DOC-08 / WS-02 / SYS-04~~ — **done, `INTEGRATION`**, see `tests/integration/documents/cross-workspace-move.test.ts` (ODE-554); one test closed all three, driven through the real `assignToWorkspace` entry point. **WS-06 and WATCH-04 turned out not to share this gap on reflection** — WS-06 is workspace-switch UI isolation (a different capability entirely, still open, `NONE`), and WATCH-04 is an *externally*-triggered move reconciled by the watcher path, which never calls `relocateDesktopWriting` at all (still open, `UNIT_ONLY` — see its row note).
10. ~~ANN-04/ANN-05 convergence test~~ — **done**, see `tests/playwright/annotation-delete-convergence.e2e.ts` (ODE-552); reframed from the spec's assumed "editor vs. sidebar entry point" (no editor-side delete UI exists) to the real fork — `deleteAnnotation` (typed) vs. `deleteStandaloneHighlight` (standalone) — both mutation-tested.
11. ~~SYNC-03~~ — **done, `PARTIAL_INTEGRATION`**, see `tests/integration/sync/sync-worker-failure-preserves-local-content.test.ts` + `tests/desktop-catalog-sync-service.test.ts` (ODE-553). Web: a real, reachable bug found and fixed (`lifecycle` stuck permanently on `"syncing"` after retries exhaust, blocking anything gated on it, e.g. `hydrateCorrectionBlocks`). Desktop: no bug — the real `.md` file substrate confirmed untouched on failure — but the catalog/Supabase remain mocked, holding the row at `PARTIAL_INTEGRATION` overall.

## Phase 3 — Critical Proofs: methodology

Writing a capability integration test is not "convert the scenario row into a test file." Follow this sequence, and produce the Proof Contract *before* writing any test code:

> **Normative companion — read before BUILD or REVIEW of any proof:** `workflow/quality/capability-proof-contract.md`. This section explains the method; that contract holds the MUST rules the Phase 3 PRs showed are not obvious from the method alone — production-path fidelity, scheduling ≠ completion, deferred-work ownership, full-contract doubles, swallowed errors, and **coverage status as an output, never a goal** — plus the escape taxonomy (`MOCKED_SEAM`, `WRONG_ASSERTION`, `NON_PRODUCTION_PATH`, `STALE_PROOF`, `ARCHITECTURE_GAP`) and the pre-upgrade checklist.

```text
Capability Scenario
  → Proof Contract (Property, Real collaborators, Allowed fakes, Given/When/Then, Failure/Then,
                    Production path: entry point + preceding transitions that reach Given,
                    Completion event: the exact point after which the invariant holds)
  → pick the minimum-sufficient runtime (Vitest by default; cargo test for the
    Rust/SQLite seam; a local Postgres/pgTAP for RLS/DB-function properties;
    Playwright only when the property genuinely lives in the DOM)
  → implement the test
  → mutation-test it: reintroduce the exact bug the property guards against,
    confirm the test goes red for that reason, then revert
  → run the pre-upgrade checklist (capability-proof-contract.md), THEN derive
    the status — the status is a conclusion of the evidence, never the target
  → update this map's coverage_status/Evidence/Note for every scenario the
    test closes (one test can legitimately close several IDs)
```

**Runtime is chosen by where the real integration lives, not by convention.** Vitest is the default and can express unit, contract, *or* integration proofs — what determines the level is which collaborators stay real, not which runner executes it. Reach for `cargo test` (real fs + real SQLite) when the property is specifically about the Rust side; a local Postgres/pgTAP harness when the property lives inside a database function or RLS policy (a Vitest mock of Supabase cannot prove either); Playwright only for properties that genuinely depend on DOM/browser choreography (scroll, focus, keyboard, drag) — and even then, try to separate the *ownership* logic (Vitest) from the *DOM mechanics* (Playwright, scoped) rather than handing the whole capability to Playwright.

**A capability test is good exactly when: if you reintroduce the failure mode it exists to prevent, it fails for the right reason.** Passing today proves nothing on its own. Five supporting checks:
1. Assert observable end-state (file content, catalog resolution, a rendered projection) — not `toHaveBeenCalled()` on the pieces you're supposed to be trusting.
2. The collaborators the *declared chain* names are real, not mocked — a real `DocumentService` calling a mocked catalog and a mocked filesystem is still `CONTRACT`, not `INTEGRATION`, no matter how good the mocks are.
3. The test crosses the boundary where bugs actually appear (the seam), not just one side of it.
4. Failure/race variants exist when that's the actual risk — happy-path-only is insufficient for anything persistence-shaped.
5. The test is deterministic — no `sleep`, no real external API, no shared/production state; temp dirs, deferred promises, and deterministic fakes only at genuinely external boundaries.

**Formal bar for declaring `INTEGRATION` (all seven, or the row stays `PARTIAL_INTEGRATION`):**
```text
1. The relevant entry point of the declared critical chain actually executes.
2. The chain's internal collaborators are real, not mocked/spied.
3. Only genuinely external boundaries are faked (network, third-party providers) —
   never an internal collaborator the chain names.
4. Observable state/artifact is verified, not just that functions were called.
5. The identified failure mode would make the test fail if reintroduced —
   verified live, not assumed.
6. The starting state is reached through the transitions production actually
   performs — not seeded — whenever those transitions can touch the property
   (otherwise the escape is NON_PRODUCTION_PATH).
7. The assertion runs after the event that establishes the invariant, not the
   one that schedules it; no error inside the chain was swallowed by production
   during the run.
```

**Test organization is by capability, not by source file** — `tests/integration/<capability-area>/<scenario-cluster>.test.ts` (e.g. `tests/integration/documents/materialize-save-reopen.test.ts`), so the unit of thought stays "what does the product need to keep doing," not "which class am I testing." One well-chosen test can legitimately close several scenario IDs at once (see DOC-03/DOC-04 below) — that's a feature, not scope creep, as long as each closed ID's Proof Contract is genuinely satisfied.

### Phase 3 progress tracker

| Scenario(s) | Status before → after | Test |
|---|---|---|
| DOC-02, DOC-03, DOC-04, DOC-06 | PARTIAL_INTEGRATION/CONTRACT → **INTEGRATION** | `tests/integration/documents/materialize-save-reopen.test.ts` |
| CONFIG-07 | PARTIAL_INTEGRATION → **INTEGRATION** | `supabase/tests/delete_vocabulary_item_rewrite.test.sql` (Postgres) + `tests/integration/vocabulary/schema-change-safety.test.ts` (desktop) |
| SHARE-04 | NONE → **PARTIAL_INTEGRATION** | `supabase/tests/writing_shares_permission_enforcement.test.sql` (table RLS + security-definer RPC, both real Postgres — DB substrate only, service-role web path still open) |
| AI-01 | NONE → **INTEGRATION** | `tests/integration/ai/suggest-title-lifecycle.test.ts` (real `webAIService` → real `localDB` → real route handler → only the external provider faked) — bug reproduced live and fixed |
| WATCH-07 | NONE → **PARTIAL_INTEGRATION** | `tests/integration/documents/watch07-external-content-conflict.test.ts` (real `PersistenceCoordinator` → real `FilesystemDocumentService` → real behavioral write-file double with the same hash-check logic as Rust, incl. a SEQUENTIAL same-document proof); `src-tauri/src/commands/document.rs` cargo tests (real Rust `write_file`); `tests/editor-external-change-policy.test.ts` (pure decision function + reason filter) — a genuine silent-overwrite gap found and closed at the write-side layer; `EditorShell`'s own reactive wiring still lacks its own test, so not yet `INTEGRATION` |
| EXP-05 | NONE → **PARTIAL_INTEGRATION** | `tests/lib/services/desktop/export-delivery.test.ts` (real `saveDesktopBinaryExport` and real `saveBinaryArtifact` desktop dispatch, both against a real temp fs; native dialog faked); `tests/components/properties-panel-export.test.tsx` (real `PropertiesPanel.handleExport`) — three real false-success bugs found and fixed (main editor: latent; Desk preview modal PDF/DOCX and Markdown: reachable); real callers not yet connected to `saveBinaryArtifact` in one test, so stays one notch below `INTEGRATION` |
| STATE-05 | NONE → **PARTIAL_INTEGRATION** | `tests/editor-session-store.test.ts` — all three real "New Artifact"/draft-reuse paths proven at the store level; found and fixed a real bug in the draft-slot-reuse case; store↔`EditorShell` DOM-application seam still open |
| ANN-04/ANN-05 | UNIT_ONLY/INTEGRATION → **INTEGRATION** (both) | `tests/playwright/annotation-delete-convergence.e2e.ts` — corrected the map's assumed "editor vs. sidebar" framing (no editor-side delete UI exists); proved the real fork (`deleteAnnotation` vs. `deleteStandaloneHighlight`) converges, both mutation-tested |
| SYNC-03 | UNIT_ONLY → **PARTIAL_INTEGRATION** | `tests/integration/sync/sync-worker-failure-preserves-local-content.test.ts` (web, real bug found and fixed: `lifecycle` stuck on `"syncing"` past retry exhaustion); `tests/desktop-catalog-sync-service.test.ts` (desktop, real `.md` file substrate proven untouched, catalog/Supabase still mocked) |
| STATE-03, STATE-04 | INTEGRATION *(STALE)* → **INTEGRATION** (revalidated) | `tests/playwright/editor-tab-scroll-restore.e2e.ts` (rewritten on `/perf/editor-harness`, `[data-editor-tab-id]`-keyed) — two test bugs fixed (never-persisted harness id, dead tab selector) surfaced a real third bug in `editor-shell.tsx`'s hydration effect (generation cancelled before its own deferred scroll-restore rAF fired); fixed and mutation-tested live (6/8 red without the fix, 13/13 green with it); a follow-up markdown-mode leak risk and a stuck `open-error` hydration gap were found and fixed in the same PR (see the STATE-04 row note) |
| DOC-08, WS-02, SYS-04 | PARTIAL_INTEGRATION/UNIT_ONLY → **INTEGRATION** (all three) | `tests/integration/documents/cross-workspace-move.test.ts` — real `assignToWorkspace` → real `relocateDesktopWriting` → real catalog/settings services, two real distinct registered BindingRoots; added `tauriRelocateFileDouble` to the shared real-desktop-doubles support module; caught on review before merge: the origin-root manifest resync was silently failing inside the double and getting swallowed by production's own recoverable-error handling, so WS-02's actual Workspace-index convergence was unproven — fixed by giving `tauriWorkspaceSyncDouble` real per-root manifest state; mutation-tested against a copy-not-move bug, a skipped-catalog-commit bug, and (after the review fix) a disabled-reconcile bug and a skipped-manifest-bind bug |

Real collaborators used: `DesktopDocumentService`, `FilesystemDocumentService`, `SqliteDocumentCatalog`, `PersistenceCoordinator` (all real, unmodified production classes) against a real temp filesystem, entering saves through the coordinator's public API (`persist()` + `settle()`, never a raw `saveWriting()` call, per the declared chain). Precise framing (not "real SQLite"): this is application-side integration with the native Tauri/Rust/SQLite boundary replaced by a real (not spy-based) behavioral test double that mirrors the Rust side's row/binding consistency rules — `RUNTIME` stays reserved for TS → actual `invoke()` → real Rust/SQLite, which SYS-05/SYS-08 still track as a separate, open gap. Allowed fakes: that native transport itself (no bridge in Vitest) and the cloud sync flush (external network boundary).

Two rounds of review found real things, exactly what this phase is for:
- **Mutation-testing, round 1:** the first fault-injection attempt on the FAILURE case passed for the wrong reason (it hit an unrelated placeholder write, not the write in the declared chain) — corrected to target the specific write by call number, then confirmed red/revert.
- **Product decision surfaced:** the FAILURE case's original assertions only checked the catalog, not the filesystem — a failed materialization can leave a real orphan `.md` file with no catalog row. Decided: accepted recoverable state owned by the reconciler (not rolled back by `persist()`); the test now asserts this explicitly instead of only checking half the state.
- **API-contract correction:** the RACE case originally invented a private completion signal around `saveWriting` to work around `persist()`'s promise resolving before its own write landed. That's not a bug — `PersistenceCoordinator` documents `persist()` as optimistic/fire-and-forget and `settle()` as the API for durability confirmation. Both DOC-03 and DOC-04 now go through `persist()` + `settle()` exclusively.

**CONFIG-07 (real collaborators):** `delete_vocabulary_item` (real Postgres function, run against a real local Supabase/Postgres instance via `pg_prove`/pgTAP — the first pgTAP test in this repo actually verified locally as part of writing it, though still not wired into CI, matching the pre-existing pattern for all pgTAP tests here) on the Postgres side; `DesktopSettingsService` (real, unmodified) against the same real behavioral catalog double from the document-lifecycle cluster, extended with a real `tauriCatalogBulkDualWrite` double, on the desktop side. Allowed fakes: same native-transport boundary as above; RLS/`security invoker` themselves are real, not simulated. Mutation-tested live on both engines — the Postgres test's first fault injection (dropping the `author_id` filter alone) didn't actually reproduce a bug, because the `writings` table's own RLS UPDATE policy independently blocks cross-user rewrites regardless of the function's WHERE clause — a genuine defense-in-depth finding. A more realistic regression (`security definer`, which bypasses RLS, combined with the missing filter) did reproduce real cross-user corruption and the test caught it. The desktop test's fault injection (dropping the key filter in `rewriteCatalogToBaseValue`) caught the bug on the first attempt.

**SHARE-04 (real collaborators):** the `writings`/`writing_shares` RLS policies and `can_read_writing()` (real Postgres, run under `set local role authenticated`/`anon` + `request.jwt.claims` per user, same technique as every other real-DB proof here) — proves a stranger (authenticated or anonymous) cannot read a private or shared-with-someone-else writing but genuinely can read a public one (the positive control matters: without it, "RLS blocks everything" would also pass a test that only checks denial); proves the owner can create a share grant under RLS and the grantee cannot create or revoke one. A second, independent authorization path for the same property, still real Postgres: `list_incoming_shared_writings()`, a `security definer` RPC desktop's "shared with me" list depends on — it re-implements its own `auth.uid()` scoping instead of relying on table RLS, so a bug there is a full cross-user leak with nothing else to catch it. Mutation-tested live on all four properties: loosening `can_read_writing` to treat `shared` the same as `public` was caught; dropping the RPC's `shared_with_id` filter was caught; loosening the share-grant delete policy to `using (true)` was caught; breaking the insert policy to `with check (false)` was caught. **Deliberately out of scope:** the web app's service-role authorization paths (`createAdminClient()` + manual checks in `web-sharing-service.ts` and its callers) bypass RLS by design and have no proof here — this is why the scenario is `PARTIAL_INTEGRATION`, not `INTEGRATION`: the declared chain is the full product outcome, and only the DB half of it is proven.

**AI-01 (real collaborators):** `webAIService.suggestTitle` (real, unmodified production singleton) → `localDB.writings` (real IndexedDB via `fake-indexeddb/auto`, seeded with each lifecycle value exactly as the app would produce it) → the real `POST` handler in `app/api/ai/title-suggestions/route.ts` (real request validation, real `hasEnoughTitleSuggestionContent` check, real prompt construction, real response parsing) → real `getAIProviderConfig()` (real env-var-driven config). **First version of this proof only connected `webAIService` to a faked `/api/ai/title-suggestions` response and left the real route handler unexercised in the same test — review correctly identified this as two separate PARTIAL proofs meeting at a mocked seam, not one INTEGRATION proof, since the internal `webAIService ↔ route` boundary is not genuinely external.** Corrected by routing a single global `fetch` mock by URL: a call to `/api/ai/title-suggestions` is turned into a real `Request` and passed to the real, imported `POST` function; a call to the real provider URL that `getAIProviderConfig()` resolves to gets a canned response. Only that last hop — the actual external AI provider — is faked; every internal collaborator between `webAIService` and the provider call now genuinely executes. Deliberately kept as deterministic test boundaries, not the AI-01 property: `getCurrentUserFromRequest` (mocked to a fixed user) and `tryAcquireAiAdmission`/`releaseAiAdmission` (mocked to always admit — the real implementation is a Postgres RPC via `createAdminClient()`, which needs a live Supabase project to run for real). Reproduced the reported bug live first (ran the strengthened test against unfixed code: `local-only`/`syncing` genuinely went red with `INVALID_INPUT` and the real route never reached — `routeCallCount` stayed 0 — while `server-confirmed`/no-`writingId` were already green) before touching production code. Root cause confirmed via `git show 8699899e` (ODE-205): the identical guard was added to `suggestTitle` and `hydrateCorrectionBlocks` in the same commit; legitimate for the latter (server-side correction blocks are keyed by `writingId`), never justified for the former (the route doesn't parse `writingId` at all). Fix scoped to `suggestTitle` only; `hydrateCorrectionBlocks`'s guard is untouched and has its own regression coverage in the same test file as a mutation-testing control. Two pre-existing unit tests in `tests/ai-auth-services.test.ts` had asserted the buggy behavior as correct since ODE-205 — updated to match the fixed contract. Mutation-tested live (second pass): re-added the removed guard, confirmed `local-only`/`syncing` failed with the real route never reached, across 3 assertions in two test files, reverted, zero diff against committed source.

**WATCH-07 (real collaborators):** the confirmed-live production chain `DesktopAppShell` → `useWorkspaceReconciler` → `ensureWorkspaceReconciler` (`desktop-workspace-reconciler.ts`) → real `watchFsPaths` (Tauri `plugin:fs\|watch`) → `reconciler.notifyRootChanged` → `reconcileRoot` (pure, already unit-tested) → `catalog.applyReconcileTransaction` → `CatalogChange` → `editor-shell.tsx`'s own `subscribeToCatalog` handler for the open document. **The investigation itself is the finding worth recording**: a first static trace followed only `DesktopWorkspaceService.watchWorkspace()`/`watchWorkspaces()`, found zero callers (correctly — they're dead code, their real callers were deleted in ODE-373/`067cee59` with no replacement), and wrongly concluded "no watcher runs in production at all." Corrected on review by finding the actual, currently-live second-generation watcher wiring (`desktop-workspace-reconciler.ts`), which the first pass had in its own earlier `grep` output but never opened. The real gap was narrower and different: the live chain reaches the open editor correctly, but the editor's own reaction only ever compared `localPresent`/`canonicalPath` — never `content_hash` — so a pure content edit was silently absorbed with no reaction at all, and separately, no layer in the write path (`PersistenceCoordinator` → `FilesystemDocumentService.saveWriting` → Rust `write_file`) had any precondition on the file's current on-disk content, so a subsequent save from either side would always silently win the race.

Real collaborators for the fix: `write_file` (Rust, extended with `expected_content_hash`, reusing the existing `content_hash_for_markdown_file`/blake3 primitive `workspace_sync` already relies on); `PersistenceCoordinator` (real — owns the durable-content-hash baseline itself via `getDurableContentHash`/`setDurableContentHash`, see below); `FilesystemDocumentService.saveWriting` (real, attaches the write's authoritative resulting hash to `WritingRecord.contentHash`, itself sourced from the same manifest-sync hash the catalog binding already tracks — not recomputed separately); `resolveExternalContentChange` (pure — the CLEAN/DIRTY/reason-filtering policy, unit-tested independent of any editor/fs/network). Allowed fakes: same native-transport boundary as every other desktop proof, extended in `tauriWriteFileDouble` to mirror the real Rust hash-check exactly.

**Three real races found and fixed across two review passes, all against the first version of this fix:**
1. **Baseline ownership.** The first version threaded a `baselineContentHash` field through `PersistenceSnapshot` itself, frozen at the moment `persist()` was called. `persist()` can legitimately resolve `true` optimistically for a request merged into an already-in-flight write (documented, pre-existing behavior — the actual write hasn't landed yet). A second save queued behind that in-flight first save would carry the *pre-first-save* baseline once it finally ran, and would see the first save's own just-written content as a "changed" file — misreading its own predecessor as an external edit and refusing to save with a false `CONFLICT`. Fixed by moving baseline ownership fully into `PersistenceCoordinator`: it tracks `durableContentHashByWritingId` internally, resolves `expectedContentHash` fresh at the moment each write actually executes (not at snapshot-construction time), and advances the tracked value only from the write's own authoritative result. Proven with a new SEQUENTIAL test (two of the app's own saves for the same document, the second queued behind the first while it's in flight) and mutation-tested live: reverting to "advance the baseline optimistically" made that test fail with exactly the false `CONFLICT` described above.
2. **Own-save misattribution.** `DesktopDocumentService.persist()` emits `CatalogChange(reason:"content")` for a normal body-only autosave (`reason:"upsert"` for a metadata change) — the *same* document whose hash the editor is watching. The first version of the editor's reaction had no reason filter at all, so its own successful autosave could trigger the CLEAN/DIRTY logic against itself. Fixed by requiring `CatalogChange.reason === "bulk"` (the reconciler's own reason, and the only one that means "a real filesystem scan observed this") before treating a hash difference as evidence of an external edit. Mutation-tested live: removing the filter made 7 decision-function assertions wrongly resolve to "conflict"/"auto-reload" for the app's own `content`/`upsert`/etc. catalog changes.
3. **The persistence-debounce window.** `hasPending()` alone under-reports "is there an unsaved edit": the desktop persistence debounce (150ms rich mode, `MARKDOWN_SAVE_DEBOUNCE_MS` markdown mode) means there is a real window right after a keystroke where the editor already holds an edit that hasn't reached `persist()` at all yet — `hasPending()` still (correctly, from its own narrower view) says nothing is pending, so a `bulk` `CatalogChange` arriving in exactly that window would classify the document CLEAN and auto-reload straight over the unsaved keystroke. Fixed with `hasUnconfirmedLocalEditRef` — set the instant a real edit happens (TipTap's own `onUpdate` for rich mode; each markdown-mutating handler's own entry point — `handleMarkdownChange`, `persistMarkdownDraft`, `applyMarkdownFromPanel`, insert-link/table/image — for markdown mode; never on a programmatic `setContent`, already guarded by the pre-existing `isApplyingContentRef`) and cleared the instant that content is actually handed to `persist()`, from which point `hasPending()` is authoritative. Combined via a new, named, tested `computeHasPendingLocalEdit(unconfirmed OR pending)` in `external-change-policy.ts` rather than an untested inline `||`. Mutation-tested live: dropping the `||`'s first operand made two new tests fail — the combined signal read `false`/`auto-reload` for exactly the "typed but debounce hasn't fired" case the fix exists for.

All three mutation-tested and reverted with zero diff. Policy decided explicitly by the user, not inferred: CLEAN auto-reloads with a brief notice; DIRTY blocks every autosave and raises a two-action conflict ("Reload external" / "Keep my version" — the latter re-seeds the coordinator's baseline to the exact external hash first, then persists normally, rather than a separate force-bypass code path) — no automatic merge. **Still open, and the reason this stays `PARTIAL_INTEGRATION`:** no automated test exercises `EditorShell`'s own reactive wiring end to end (a real `CatalogChange` arriving → the real effect → `editor.commands.setContent()` → the conflict banner's real button handlers) — that would need rendering the full component or extracting a smaller controller/hook, deliberately deferred rather than rushed in this pass.

**EXP-05 (real collaborators):** `saveDesktopBinaryExport` (real, unmodified) → real `tauriWriteBinaryFile` (doubled to perform a genuine fs write — mkdir parents, write `.tmp`, rename — mirroring the real Rust `write_binary_file` command exactly, against a real temp directory) — proves dialog-cancel (`false`, the writer never invoked), real success (exact bytes land on disk), and a genuine fs write failure (a parent path component that is a plain file, not a directory — a real, deterministic, cross-platform mkdir/write error, not a scripted throw) all propagate correctly. Also, since review, `saveBinaryArtifact` (`lib/utils/download.ts`, real) → real desktop dispatch (`isDesktopRuntime()` driven by a real injected `window.__TAURI_INTERNALS__`) → the same real `saveDesktopBinaryExport`/fs path — closing the one hop below the caller. Paired with `PropertiesPanel.handleExport` (real, rendered via `react-dom/client` + `act`, only the three export-callback props faked) proving the UI reaction: success only on an explicit `true`, nothing shown on `false`/`void`, an error shown and never success on rejection. Allowed fakes: the native OS save dialog (unautomatable) and the Tauri IPC decode step.

**Three real bugs found and fixed, the third by extending the investigation to every export call site:**
1. `editor-shell.tsx`'s `exportBinary()` returned bare `undefined` (not `false`) on its `!currentWritingId` early return — a real contract violation of "success only if written," but **latent**: the PDF/DOCX menu items are `disabled={!hasRemoteWriting}`, and `hasRemoteWriting` requires the same `writingId` truthy in the same render, so the branch is provably unreachable via the main editor's own UI today. Fixed anyway (`return false`) as defense-in-depth against a future caller without that guard. Mutation-tested live: reverting to a plain `return` reproduced the false-success text in `properties-panel-export.test.tsx`'s regression test.
2. `PropertiesPanel.handleExport`'s own guard was inverted relative to the invariant: `if (exported === false) return` shows success unless explicitly `false`, but the prop type (`Promise<boolean \| void>`) permits `void` — so a caller resolving `void` (not just the two booleans) would wrongly show success. Fixed to `if (exported !== true) return` (opt-in to success). Mutation-tested live: reverting to the `=== false` check reproduced the same false-success text.
3. Investigating whether the same invariant held at *every* call site (not just the one named in the original Proof Contract) surfaced a **third, independently reachable** instance, in both its export formats: `app/(app)/desk/page.tsx`'s `exportWritingDocument` called `await saveBinaryArtifact(result.data)` with no `return`, discarding the boolean; `downloadWritingMarkdown` (in both `desk/page.tsx` and `collections-view.tsx`, which share the same bug) returned nothing on its own early-return-when-no-payload branch either. `WritingPreviewModal.handleExportDocument`/`handleExportMarkdown` had no branch distinguishing cancel from success at all, showing "PDF exported."/"DOCX exported."/"Markdown exported." on any non-throwing resolution. Unlike bug 1, this is reachable via a normal click in Desk's own preview modal (not gated the way the main editor is) — for PDF/DOCX and for Markdown alike. Fixed the same way as bug 2, for all four functions: each now returns the boolean explicitly, `WritingPreviewModal`'s `onExportDocument`/`onExportMarkdown` prop types widened to `Promise<boolean \| void>`, `handleExportDocument`/`handleExportMarkdown` both gained the identical `!== true` guard. Mutation-tested live on the `saveBinaryArtifact` dispatch hop (reverting the desktop branch to discard the boolean reproduced a false `true`); the `WritingPreviewModal` handlers themselves could not be mutation-tested live — see the scope note below.

**Scope note — why this stays `PARTIAL_INTEGRATION`, not `INTEGRATION`:** two real gaps, not one.
- **The internal seam between the real caller and the proven `saveBinaryArtifact` layer is still faked in every test — the same rule applied to AI-01.** `export-delivery.test.ts` proves `saveDesktopBinaryExport`/`saveBinaryArtifact` against a real fs; `properties-panel-export.test.tsx` proves `PropertiesPanel.handleExport`'s reaction to a *faked* export callback. Two partial proofs meeting at a mocked seam are not one integration proof. The declared gap: `real caller (exportBinary/exportMarkdown in editor-shell.tsx, or exportWritingDocument/downloadWritingMarkdown in desk/page.tsx) → real export orchestration → saveBinaryArtifact → saveDesktopBinaryExport → native boundary fake` — only the last hop should be faked, and today the caller hop is faked too. Not closed in this pass: `editor-shell.tsx` is a 6000+-line component (same cost class as WATCH-07's deferred `EditorShell` wiring test), and `desk/page.tsx`'s callers are closures inside an equally large page component, not extractable without a larger refactor judged out of scope here.
- **`WritingPreviewModal`'s own fix (bug 3, all four affected functions/formats) has no DOM-level test of its own.** Driving this component past a real click into `await`ed settlement hits a reproducible `act()` hang, confirmed independent of this change (reproduces identically with a trivial `vi.fn().mockResolvedValue(true)` fake, and independent of whether `preview` is null or populated — ruling out both the earlier `hasRemoteWriting` hypothesis and an unrelated mock-identity bug found and fixed along the way in `useWritingPreviewCache`'s test double). `preview-modal.test.tsx` never surfaces this today because it never drives a click. Diagnosing that harness issue was judged disproportionate to this proof (same class of deferral as WATCH-07's `EditorShell` wiring gap) and the user explicitly accepted the gap rather than blocking on it.

Both gaps are tracked as explicit follow-ups below rather than closed by scope creep in this pass.

Remaining P0 items (per the earlier reviewer-proposed list): ~~STATE-05~~ (store-level portion closed, `PARTIAL_INTEGRATION`, ODE-551 — store→`EditorShell` wiring proof still open), ~~ANN-04/ANN-05 convergence~~ (closed, `INTEGRATION`, ODE-552), ~~SYNC-03~~ (closed, `PARTIAL_INTEGRATION`, ODE-553 — real bug fixed on web, desktop catalog mock still open), ~~WS-02/DOC-08 (+SYS-04)~~ (closed, `INTEGRATION`, ODE-554 — WATCH-04 turned out to be a distinct, still-open gap; see its row), ~~STATE-03/STATE-04's E2E proof needs revalidation~~ (closed, `INTEGRATION`, ODE-555 — a real third bug found and fixed: a hydration-generation cancellation race silently dropped the scroll restore in ~30-50% of runs, plus a follow-up markdown-mode leak risk and a stuck `open-error` hydration gap found on review; see those rows). Open follow-ups from prior passes: WS-06 (workspace-switch isolation) and WATCH-04 (externally-triggered cross-root move) remain open.

Follow-ups opened by review of this phase's own proofs (small, scoped, not blocking the P0 list above):
- **CONFIG-07 branch coverage** ([ODE-547](https://linear.app/hugo-marin/issue/ODE-547)): the current proof only exercises the `kind = status` deletion branch (custom status used by documents → rewritten to `draft`). The `kind = type` branch (custom artifact type → rewritten to `general`) shares the same function/service but has no test of its own — table-driven coverage for both branches, on both Postgres and desktop, closes this properly. Related: no `cargo test` proves `catalog_bulk_dual_write`'s SQLite transaction actually rolls back an in-flight batch when a later row fails (the existing desktop test only proves nothing is applied when the failure is armed before the batch starts, not mid-batch atomicity).
- **SHARE-04 service-role path:** a real proof (integration test against a real Postgres instance, admin client, through `web-sharing-service.ts` or the routes that call it) that the manual authorization checks in the service-role paths listed above are actually enforced — needed before SHARE-04 can move to `INTEGRATION`.
- **pgTAP CI wiring:** `CONFIG-07` and `SHARE-04`'s Postgres proofs are the two most valuable tests this phase produced, and neither runs in CI — a path-filtered job (`supabase/migrations/**`, `supabase/tests/**` → boot local Postgres → `supabase test db --local`) would catch a schema/RLS regression before merge without paying the Postgres-boot cost on every PR.
- **`WritingPreviewModal` export wiring test, and the underlying `act()` hang:** EXP-05's Desk-modal fix has no DOM-level test — rendering this component and awaiting past a real click hangs indefinitely in `act()`, reproducibly, independent of the fix itself and independent of `preview`/`hasRemoteWriting` state. Worth its own investigation (separate from any specific proof) since it likely blocks other future tests of this same component, not just this one.
- **EXP-05 internal seam — real caller not yet connected to `saveBinaryArtifact`:** `editor-shell.tsx`'s `exportBinary`/`exportMarkdown` and `desk/page.tsx`'s `exportWritingDocument`/`downloadWritingMarkdown` are still only proven at the unit/prop-callback level (`properties-panel-export.test.tsx`) or not proven at all as real callers into `saveBinaryArtifact`. Closing this needs either an `editor-shell.tsx` render (expensive, same class as the WATCH-07 wiring gap) or extracting these functions into testable units — needed before EXP-05 can move to `INTEGRATION`.

## CI candidates (cheap, deterministic, high-value — matches the spec's §19 criteria)

- EXP-04 / SYS-02 (already CI-suitable in shape; formalize as an architecture/contract test if not already blocking).
- SYNC-08 metadata-only safety.
- STATE-03/04 view-state isolation (already E2E against the real dev server, not a packaged bundle — fits the existing `scoped-ci.yml` cost tier from the CI restructuring work, not universal blocking).
- ANN-04/ANN-05 convergence test once implemented.
- **`supabase/tests/*.test.sql` (pgTAP)** — implemented (CONFIG-07, SHARE-04) but not yet CI-wired; a path-filtered job on `supabase/migrations/**`/`supabase/tests/**` running `supabase test db --local` would close this without adding Postgres-boot cost to every PR. See the Phase 3 tracker's "pgTAP CI wiring" follow-up.

## Non-CI proofs (correctly excluded from universal blocking)

- VOICE-08's real DMG build/scan (already release-only, correct).
- Anything requiring a real microphone, a real packaged desktop smoke test, or hardware-specific behavior.
- The manually-flagged `PENDING` rows in `tests/fase9-document-catalog-invariants.test.ts` (offline/restart convergence, Finder-driven rename/move, conflict/ambiguity resolution) — these are honest team-acknowledged DMG-only gaps, not silent ones.

---

*This map is a durable artifact, not a one-time audit — update `coverage_status` and `Evidence` in place as tests are added (and bump the audit snapshot date/commit for that row), rather than re-auditing from scratch. Planning/BUILD/Review integration (declaring affected capabilities in the Issue Brief, a BUILD capability-proof step, Review evaluating capability preservation) and CI promotion are follow-up phases, deliberately not done in this pass.*
